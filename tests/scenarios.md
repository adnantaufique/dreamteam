# Dreamteam — Validation Scenarios (subagent tests)

Each scenario is run by dispatching a fresh **`general-purpose`** subagent, giving it the named skill file(s) (or the installed skill) + the input, and asserting its output matches **Expected**. Re-run after any edit. There is no code compiler — *the subagent's behavior is the test*. Where noted, **judge coherence, not exact agent picks**.

Install first so installed-skill scenarios see the latest copy: `pwsh ./install.ps1` (Windows) or `bash ./install.sh` (Linux/macOS) — publishes `skills/dreamteam/` → `~/.claude/skills/dreamteam/`.

## S1 — profile fast-path (`references/profiles.md`)
- **Input:** `profiles.md` + task "build an Android TV app".
- **Expected:** the **mobile-dev** dreamteam — producer Mobile App Builder (+ UI Designer for design), gate **[Code Reviewer, Reality Checker]**, `sequential`. Each role carries its default tier (Mobile App Builder@standard, UI Designer@cheap, reviewers@capable; abstract tiers, resolved per platform).

## S2 — Caster agent, no profile (`references/caster.md`)
- **Input:** `caster.md`; act as the Caster agent for task "harden and load-test our REST API" (no `--profile`/`--roster`).
- **Expected:** a coherent dreamteam manifest — a planner; producer(s) (e.g. Backend Architect / API Tester / Performance Benchmarker / Security Engineer); `reviewers` **including Reality Checker**; `gate_policy`; `workstream_strategy`; one-line rationale per pick. *Judge coherence, not exact picks.*

## S3 — gate synthesis + honesty (`references/gate.md`)
- **Input:** `gate.md` + two mock reviews: (1) Code Reviewer "Approve-with-changes" — a real **High** (resource leak, found by code-read) + a **predicted High** (build break); (2) Reality Checker "PASS" whose green build **refutes** the predicted build break and which did **not** exercise the leak path.
- **Expected:** keeps the leak as the surviving **must-fix**; **drops** the refuted build-break (evidence beats prediction); verdict = **fix-then-pass**.

## S4 — core loop + isolation (`references/loop.md`)
- **Input:** `loop.md` + `gate.md` + a 2-workstream **parallel** plan (both producers mutate the same server file) + a parallel manifest.
- **Expected:** describes **produce→gate→fix→integrate** per workstream; gives **each parallel file-mutating producer its own git worktree**; surfaces **each** workstream's gate verdict; integrates only on pass; needs-work → stop + escalate.

## S5 — end-to-end dry-run (installed `SKILL.md`)
- **Input:** installed skill; invocation `/dreamteam "add OAuth login to our web app" --autonomy confirm`. Dry-run (no execution).
- **Expected:** resolves + prints the **web** dreamteam (+ Security Engineer for auth, + Reality Checker) with rationale; describes the produce→gate→fix→integrate loop; **stops for confirmation, does not execute**.

## S6 — model-tier assignment (`references/caster.md`)
- **Input:** `caster.md` + a mixed task with a clear mechanical sub-task, an integration sub-task, and review — e.g. "add a config constant (1 file) and refactor the auth middleware across the service, then review"; no `--profile`/`--models`.
- **Expected:** the manifest assigns the **mechanical role → cheap**, the **integration role → standard**, **reviewers → capable** (incl. Reality Checker), **max not used as a default**, and each rationale line names the tier. *Assert the tiers exactly (cheap/standard/capable); judge only the agent-name picks for coherence.*

## S7 — tier escalation (`references/loop.md` + `references/gate.md`)
- **Input:** `loop.md` + `gate.md` + a single workstream whose first producer (dispatched at **cheap**) returns output the gate rules **needs-work** (a real High that isn't fixed); plus a `NEEDS_CONTEXT` variant.
- **Expected:** re-dispatches the fix **one tier up (cheap→standard)**, capped at `max_fix_iterations` then escalates to the human; reports the **tier path** (standard→…); the `NEEDS_CONTEXT` variant stays at **cheap** + adds context (no tier bump).

## S8 — retro learnings (`references/retro.md`)
- **Input:** `retro.md` + a mock run record (two workstreams: one passed clean; one escalated cheap→capable after a needs-work on a resource leak).
- **Expected:** emits a learnings record matching the schema, with **evidence-tagged `change` deltas** (e.g. `{target:tier, delta:"default the leak-prone role to capable", evidence:"escalated twice on the leak"}`); flags any skill-editing delta as **proposed/human-gated**; records nothing it can't cite run evidence for.

## S9 — Caster consults learnings (`references/caster.md` + `references/learnings.md`)
- **Input:** `caster.md` + a `learnings.md` seeded with one entry (`android | keep the ExoPlayer-leak check as a must-fix; default the build role to capable | evidence: escalated twice`). Task "build an Android TV app".
- **Expected:** adjusts the **mobile-dev** pick accordingly (the `android` learnings key normalizes to `mobile-dev`; build role → capable; leak check noted) **and** treats it as an **overridable default** (states it could override), not a hard rule.

## S10 — Layer B evolve dry-run (`references/evolve.md`)
- **Input:** `evolve.md` + an ai-research task + `--evolve`. Dry-run (no execution).
- **Expected:** describes the generations loop with `[methodology reviewer, Reality Checker]` per generation, a budget, the human gate, and the leakage caution — **without executing** — and confirms it's opt-in / ai-research-only.

## S11 — cross-CLI tier + dispatch resolution (`references/platforms.md`)
- **Input:** `platforms.md` + these asks: (a) resolve **tier=cheap** for Claude Code, Gemini, Codex, CodeWhale, OpenCode, Cursor; (b) resolve **tier=max** for all six; (c) back-compat: resolve a role carrying the Claude name **`sonnet`** on Gemini.
- **Expected:** (a) `haiku` / `flash-lite` / `gpt-5.x-mini` / `deepseek-v4-flash` / **OpenCode → the configured provider's mini / native `small_model`** (provider-agnostic, **not pinned** — e.g. `anthropic/claude-haiku-4-5` or `openai/gpt-5-mini`) / **Cursor → the picker's mini/flash rung** (picker-resolved, **not pinned** — e.g. `gpt-5-mini` or a Flash/Haiku), each with the right dispatch verb (Agent tool `model:` / `@generalist` frontmatter `model:` / Codex custom agent `model=` / CodeWhale subagent via config.toml / **OpenCode subagent via the `task` tool / `@<agent>` with frontmatter `model: <provider>/<id>`, or JSON under `agent` in `opencode.json`** / **Cursor subagent via the `Task` tool, background (`is_background: true`), with frontmatter `model: <picker-id>` or `inherit`**); (b) `fable` / `gemini pro (+high)` / `gpt-5.5 (+high model_reasoning_effort)` / `deepseek-reasoner` / **OpenCode → the provider's top reasoning model (+ effort where exposed)** / **Cursor → the picker's top reasoning model (`-high` variants as separate model IDs)**; (c) `sonnet→standard→gemini flash`. OpenCode tiers are **provider-resolved from `opencode.json`**, Cursor tiers **picker-resolved from the user's plan/picker** — **never pinned to a vendor**. A single-tier pass is insufficient — ≥2 tiers + the back-compat input must hold.

## S12 — orchestration adherence (`SKILL.md` §"You are the conductor" + `references/loop.md`)
- **Input:** give a subagent the installed/fixed skill + a multi-workstream task with an *easy* workstream (e.g. GET /health + a request-id middleware + a **one-line README badge**). Ask it, as the conductor, whether it produces the easy badge workstream **inline** or **dispatches** a producer for it — and to cite the governing rule.
- **Expected:** it answers **dispatch for EVERY workstream (including the easy badge)**, cites the imperative conductor rule + the "trivial → inline" red flag that counters it, and notes the mandatory "Dispatching … for WS-N" re-anchor; if dispatch is genuinely unavailable it **says so and pauses**, never silently producing inline.

> The tier vocabulary is abstract (`cheap/standard/capable/max`, resolved per `references/platforms.md`); re-run **S1–S7** for selection/gate/escalation, **S8–S10** for the retro/learnings/evolve loop, **S11** for cross-CLI resolution, and **S12** for conductor adherence.

## S13 — minimal-code principle (native) + ponytail amplifier (`SKILL.md` + `references/loop.md`/`gate.md`/`caster.md`)
- **Input:** a code task (e.g. "add a JSON config loader to our CLI"). Consider both: (a) `ponytail` absent, (b) `ponytail` installed.
- **Expected:** in **both** cases the producer aims for the *simplest solution that fully works* and the gate's Code Reviewer flags **over-engineering** — never by cutting validation/security/accessibility — because minimal-code is **native** to dreamteam (SKILL.md Overview + loop.md + gate.md), NOT gated on the skill. When `ponytail` IS installed, the Caster additionally attaches it to the code producer's `skills[]` as the enforcer. Absent ponytail ≠ no minimal-code check.

## S14 — advisory recommend (skills-only) (`references/recommend.md` + `references/caster.md`)
- **Input:** `recommend.md` + `caster.md`; act as the Caster for a task whose best-fit skill is **discoverable-but-not-installed** (via `find-skills`/skills.sh), with a weaker skill already installed. Plus a variant where **nothing** clears the quality bar.
- **Expected:** records a `recommendations[]` entry (a **verified** candidate + `why` + `trust` + `install` cmd + **`proceeding_with` naming the alternate it ran with, or `'omitted'`**), **proceeds with the best available** installed option, and **never blocks**. In the variant where nothing clears the bar → **no** recommendation (omitted beats a weak pick). The Caster still only selects — it recommends, never installs.

## S15 — awesome-claude-code CSV (Claude-only) (`references/recommend.md` + `references/platforms.md`)
- **Input:** `recommend.md` + `platforms.md`; a **Claude-Code** task needing a **non-skill** resource `find-skills` can't surface (e.g. a hook) → the Caster discovers from `THE_RESOURCES_TABLE.csv`. Plus the same ask under a non-Claude `--platform` (gemini/codex/codewhale/opencode/cursor).
- **Expected:** the recommendation is sourced from the CSV with **`installable:false`** (the `install` field **omitted** — no synthesized `npx skills add`), **health-flag-filtered** (skip rows with `Stale=TRUE`/`Removed From Origin=TRUE`/`Active=FALSE`), with licenses surfaced honestly. Under **any non-Claude `--platform`** the Claude-specific recommendation is **suppressed**.

## S16 — opt-in installer role (`references/recommend.md` + `references/gate.md`)
- **Input:** `recommend.md` + `gate.md`; a recommended candidate **with explicit user approval** → a gated `setup` producer (`general-purpose@cheap` + `find-skills`). Cover: (a) the pinned cmd carrying no `-y`; (b) a `-y` invocation that lands the right package; (c) `--autonomy auto`; (d) a substituted/typosquatted package.
- **Expected:** (a) the `setup` producer runs the **ONE pinned approved cmd (no `-y`)**, the gate verifies **presence + identity/source + that the command carried no `-y`**, the conductor **re-casts** (≤ once per approved set) and the skill attaches; (b) a `-y` invocation is **rejected (needs-work) even if the right package landed** (the gate checks the command, not just the result); (c) `--autonomy auto` does **NOT** bypass the human approval / auto-install; (d) a substituted/typosquatted package **fails** the identity gate.

## S17 — F6 (Karpathy fold-in) (`references/loop.md` + `references/gate.md`)
- **Input:** `loop.md` + `gate.md`; a producer dispatched with an **explicit, verifiable success criterion**, whose output exhibits **both** over-engineering **and** gratuitous diff scope (unrequested refactors/reformatting of untouched code); plus a variant with **genuine ambiguity** in the spec.
- **Expected:** the dispatch carries the explicit success criterion (what the gate will check — not "make it work"); the gate raises **both** `over-engineering` **and** `gratuitous diff scope` as findings; on genuine ambiguity the producer returns **NEEDS_CONTEXT** (surface assumptions first) — **not a silent guess**.

> S14–S17 are additive (selection/gate/loop/principles unchanged; the Caster still only selects — now also recommends); re-run S1–S13 as regression.

## S18 — audit/bugs (`references/audit.md` + `references/gate.md` + `references/loop.md`)
- **Input:** `audit.md` + `gate.md` + `loop.md`; an `audit` run (mode `bugs`) over existing code — dimension reviewers (e.g. correctness/security/performance) fan out, plus a bare `--profile audit` variant with no mode given.
- **Expected:** the dimension reviewers fan out **as producers** (read-only finding-hunters, not the gate); the gate **confirms a real bug and refutes a false positive** (evidence beats prediction); the run is **read-only — `integrate` is a no-op** (no worktree merge, nothing written back); a refuted finding is **dropped from the report, not the whole report**; bare `--profile audit` defaults to **`bugs`**.

## S19 — audit/map + depth (`references/audit.md`)
- **Input:** `audit.md`; an `audit` run (mode `map`) over a repo, run at `--depth shallow` and again at `--depth exhaustive`.
- **Expected:** mode `map` produces a **structured project model** (not a bug list); `--depth shallow` vs `exhaustive` **changes fan-out breadth**; **`exhaustive` is budget-printed + confirm-gated**; fan-out is **capped by `audit_policy.max_parallel_reviewers`**.

## S20 — mutation/mock-integrity (`references/gate.md`)
- **Input:** `gate.md` + a producer's test suite that is **green**, including (a) a test that **passes against a deliberately broken implementation** and (b) a test that **mocks the very unit under test**.
- **Expected:** both are a **must-fix** — a test that **survives a broken implementation** or **mocks the unit under test** proves nothing, **even though it's "green"** (passing ≠ verifying).

## S21 — devil's-advocate-on-unanimous (`references/gate.md`)
- **Input:** `gate.md` + (a) a **3-reviewer unanimous PASS**, (b) a **2-reviewer** unanimous pass, and (c) the same 3-reviewer unanimous pass **outside the `audit` profile**.
- **Expected:** (a) the unanimous PASS triggers **one** adversarial reviewer **once** (outside the fix loop), whose **unevidenced prediction is dropped** on reconciliation; (b) a **2-reviewer** pass does **NOT** trigger it (threshold is 3); (c) it is **off by default outside the `audit` profile**.

## S22 — mobile-dev rename (`references/profiles.md`)
- **Input:** `profiles.md` + tasks "build an Android TV app", "build a React Native app", "an iOS app"; plus `--profile android`; plus a `learnings.md` row keyed `android`.
- **Expected:** all three tasks **resolve to `mobile-dev`** (keyword widening across Android/React Native/iOS, **not** the Caster fallback); `--profile android` **resolves to `mobile-dev`** (alias kept for back-compat); a learnings row keyed `android` **still matches** the renamed profile.

> S18–S22 are additive (audit profile + gate sharpenings + the mobile-dev rename); re-run S1–S17 as regression.

## S23 — execution mode (F10) (`SKILL.md` + `references/loop.md` + `references/platforms.md`)
- **Input:** a dreamteam run reaching its **first dispatch**, on **Claude Code** and (variants) on **Gemini** and **OpenCode**.
- **Expected:** the conductor dispatches in the **background** (never a foreground/inline Agent call); on the **first dispatch** it asks **once** "background subagents or Workflow? [background]" and uses that choice for the **session** (defaults to **background** if no answer); the mode is **surfaced in the re-anchor line**; the **Workflow option is offered only on Claude Code** (Gemini/Codex/CodeWhale/OpenCode/Cursor → background, **no prompt**); the conductor still **awaits each result then gates** (background ≠ fire-and-forget); best-effort if the choice falls out of context (**re-ask, don't assume**). On **OpenCode**, "background" means dispatched via the native **`task` tool / `@<agent>`** (not the Workflow tool) — OpenCode's `task` dispatch is **synchronous/modal in core**, yet the conductor still awaits each result then gates, so the contract is unchanged (true non-blocking background only via a community plugin).

## S24 — session stickiness + carve-out (F11) (`SKILL.md`)
- **Input:** after a dreamteam run: (a) a second artifact-producing task; (b) a pure Q&A; (c) a genuinely tiny edit (one-line typo, **not** part of any plan); (d) a one-line change that **IS** a unit of the current plan; (e) the user says "stop using dreamteam this session".
- **Expected:** (a) runs **through dreamteam** (sticky); (b) **answered directly** (out of scope — no artifact); (c) **MAY** be inline via the carve-out **only after** the conductor **states the classification** and it passes **ALL** bounds (≲5 lines / one file / no new logic / no security-validation-a11y), any doubt → **dispatch**; (d) **dispatched** (a small *workstream* is not the carve-out); (e) stickiness **off for the session**, a later `/dreamteam` **re-arms**.

## S25 — `--execution` flag pre-sets the session mode (`SKILL.md` + `references/loop.md` + `references/platforms.md`)
- **Input:** a dreamteam run reaching its **first dispatch**: (a) `/dreamteam … --execution workflow` on **Claude Code**; (b) `/dreamteam … --execution background`; (c) `/dreamteam … --execution workflow --platform gemini`; (c′) `/dreamteam … --execution workflow --platform opencode`; (d) the **same run with no `--execution` flag**.
- **Expected:** the flag **IS the answer** to the one-time choice, so the conductor **does NOT ask** "Background subagents or the Workflow tool?" — (a) session = **Workflow** mode (Claude-only), prompt **skipped**, mode **surfaced in the first re-anchor line**; (b) session = **background**, prompt **skipped**, surfaced the same way; (c)/(c′) `workflow` is **INVALID on a non-Claude platform** (no Workflow tool) → **noted clearly + falls back to background subagents** — this holds for **`--platform gemini` (c) and `--platform opencode` (c′) alike**; (d) **no regression** — with no flag the one-time per-session prompt **still governs** (asked once, **default background**, per loop.md). The flag **only pre-sets the existing one-time session choice** — dispatch is still **always background**, there is **no per-workstream re-prompt**, and the produce→gate→fix→integrate loop is **identical**.

> S23–S25 are additive (F10 execution mode · F11 session stickiness · the `--execution` flag); re-run S1–S22 as regression.

## S26 — bundled-agent load + attribution (`.claude-plugin/plugin.json` + `install.sh`/`install.ps1` + `THIRD_PARTY_NOTICES.md`/`NOTICE`)
- **Input:** the installed plugin + its `plugin.json` `agents[]`, the `vendor/` tree, the install dep-check output, and `NOTICE` / `THIRD_PARTY_NOTICES.md`.
- **Expected:** `plugin.json` `agents[]` registers the **21 vendored agents under `vendor/`** — **12** agency-agents + **4** ECC (incl. the renames `methodology-reviewer`, `Security Engineer`) + **5** SuperClaude — and `mle-workflow` **auto-loads from `skills/mle-workflow/`** (no manifest entry); all load with **no external install**. The install dep-check lists them under **`bundled (ship with the plugin)` → `[ok bundled]`**, **never `[ ! ]`**; `depended-on` (the 5 superpowers sub-skills + `find-skills` + `ui-ux-pro-max`) and `optional`/`recommend-only` (ai-research skills; `Explore`/`general-purpose` built-in) are **separate groups**. Attribution holds: each vendored source (`vendor/{agency-agents,ecc,superclaude}/`) keeps a **verbatim upstream `LICENSE`** + a matching **`THIRD_PARTY_NOTICES.md` entry** (pinned commit + SPDX + MIT text + file list); renamed ECC agents are **body-pristine + carry an `origin:` line**; **superpowers / ui-ux-pro-max / graphify have NO notices entry** (depended-/recommend-only); root `LICENSE` is **unchanged Apache-2.0**.

## S27 — ml-dev profile (`references/profiles.md` + `references/gate.md`)
- **Input:** `profiles.md` + `gate.md` + task "build a PyTorch training pipeline with a dataset loader and an eval harness".
- **Expected:** resolves the **ml-dev** dreamteam — build → AI Engineer / **python-expert@standard** composing `[mle-workflow, ponytail, karpathy-guidelines]` · build-fix → **build-error-resolver / pytorch-build-resolver@standard** (training / CUDA errors); gate = **methodology-reviewer@capable** (ML pipeline · eval harness · data-contract) + **Reality Checker@capable** (data↔claim; eval **non-vacuous** — perturb to confirm) (+ Security Engineer@capable if infra/data-sensitive); `sequential`. Distinct from **ai-research** (research-salvage / paper / hypothesis). `mle-workflow` + `pytorch-build-resolver` are **bundled** (present by default); `python-expert` is used when bundled, else recommend-only — the gate is unchanged either way.

## S28 — debug profile (`references/profiles.md` + `references/gate.md` + `SKILL.md` spine step 2)
- **Input:** `profiles.md` + `gate.md` + `SKILL.md` + task "this test fails with a NullPointer — fix it" (a stack trace).
- **Expected:** resolves the **debug** dreamteam and — per the **SKILL.md spine step-2 carve-out (the same one as `audit`)** — **skips the wrapper + plan-writing**, going **reproduce-first** straight to investigate→fix (the failure *is* the spec). investigate → **root-cause-analyst@capable** `[systematic-debugging]` (absent → investigates natively + a `recommendations[]` entry, **never a block**) · fix → host code producer `[ponytail, karpathy-guidelines]`; gate = **Reality Checker@capable reproduce-then-resolve** (the failing case is **RED before / GREEN after** + a regression test) + Code Reviewer@capable; a "fix" whose test **passes even against the original unfixed code** is **rejected as vacuous**, never a pass; **unlike audit, debug integrates** (it lands the fix); `sequential`.

## S29 — ux-designer profile (`references/profiles.md` + `references/gate.md`)
- **Input:** `profiles.md` + `gate.md` + (a) "redesign our settings screen / audit its a11y"; (b) greenfield "design a component library from scratch".
- **Expected:** resolves the **ux-designer** dreamteam — UI Designer@standard composes `[ui-ux-pro-max]` (a **recommended** skill; absent → UI Designer designs natively + a `recommendations[]` entry); (a) the **redesign** sub-role adds **deep-research-agent@standard** to read the existing UI/codebase (**NO Understand-Anything**), (b) **greenfield omits it** (N/A, not degradation); gate = **Reality Checker@capable that re-derives a11y evidence** (contrast ≥4.5:1 · visible focus · `prefers-reduced-motion` · semantic structure · spec↔rendered) + a UX/design reviewer + an accessibility reviewer, **a11y NON-WAIVABLE even when `ui-ux-pro-max` is absent**, + Code Reviewer **only if implementation code is emitted** (the gate branches **spec-only vs code-emitted**); `sequential`.

## S30 — tutor profile (`references/profiles.md` + `references/gate.md`)
- **Input:** `profiles.md` + `gate.md` + (a) "explain how TCP congestion control works / ELI5"; (b) "map this codebase / understand this repo".
- **Expected:** (a) resolves the **tutor** dreamteam — understand → **deep-research-agent@standard** · explain → **Technical Writer@standard**, **no external skill** (self-contained; Understand-Anything discarded; if deep-research-agent is recommend-only, fall back to host + `recommend.md` and flag); gate = **Reality Checker@capable explanation↔source** (every claim traces to a cited source; **SOURCE WINS on conflict** — no lies-to-children: a *false* simplification is a must-fix) + a clarity reviewer (reading level · jargon defined on first use · ≥1 worked example), **accuracy gating first**; `sequential`. (b) "map the codebase / understand this repo" stays → **audit**, **not** tutor.

## S31 — karpathy-guidelines composed onto code producers (`SKILL.md` Composes + `references/caster.md` + `references/profiles.md`)
- **Input:** a code task ("add a typed retry helper to our HTTP client"); consider (a) `karpathy-guidelines` absent, (b) installed.
- **Expected:** like `ponytail`, **code-producing producers compose `karpathy-guidelines`** (surface assumptions · surgical/minimal-diff changes · verifiable success criteria), attached to their `skills[]` on **both the fast-path and the agent path**, each `find-skills`-gated **independently**; (a) absent → the producer works natively and the principle **still holds** (its guidelines already ride in `loop.md`/`gate.md`) — **degrades gracefully, never a block**; (b) installed → attached as the explicit enforcer **alongside `ponytail`**. Non-code roles (ai-research *writing*, tutor, ux-designer *design* sub-role) do **not** carry the code enforcers.

## S32 — no-glazing / objective standard (`SKILL.md` + `references/gate.md` §3 + `references/caster.md` dispatch brief)
- **Input:** `gate.md` + `SKILL.md` + `caster.md` + a mock review that returns a praise-padded **"looks good / LGTM"** with no specific finding (variant: a real Critical softened into a gentle suggestion).
- **Expected:** a bare "looks good" with no specific finding is **not an assessment and not a passing review** (non-conformant); **severity is set by impact, not tone** — a Critical stays Critical amid strong work, and a fix-then-pass is **not rounded up to a pass**; this **no-glazing / objective** standard is **distinct from the evidence-honesty rule** (tone vs proof — both required, neither substitutes); it **rides on every reviewer dispatch via the Caster dispatch brief**, so it binds **whatever agent is cast (vendored or host)** — **vendored bodies stay pristine**, the standard is never written into the agent file; the **conductor's own synthesis** also reports each verdict plainly (no softening).

## S33 — verification-loop 6-phase gate appendix (`references/gate.md` §5)
- **Input:** `gate.md` + a code workstream; ask the verifying reviewer to run the §5 checklist on a stack where one phase **genuinely doesn't apply** (e.g. `types` on an untyped script).
- **Expected:** the appendix runs, **in order**, **build → types → lint → tests+coverage → secret-scan → diff-review → PASS/FAIL**, each with **per-phase evidence**; coverage comes from a **real run** + non-vacuous tests; the inapplicable phase is **labelled `skipped` with a reason — never silently faked green**; **PASS only when every *applicable* phase passes**, any failing phase is a must-fix; it is a checklist the verifier **MAY** run, **not new gate machinery** — the verdict still comes from §2; the **ECC attribution credit** ("Adapted from ECC verification-loop, MIT © 2026 Affaan Mustafa; attribution in `THIRD_PARTY_NOTICES.md`") is **present**.

## S34 — graphify `--graph on|off|auto` (`SKILL.md` + `references/loop.md` §Graph + `references/caster.md`)
- **Input:** `SKILL.md` + `loop.md` + a dev run; cover `--graph auto` (graphify installed / absent / oversized repo), `--graph off`, `--graph on`.
- **Expected:** the graph is **AST-only** (deterministic → free to build/refresh; graphify's semantic/LLM extraction is **not** used); **G0 init** once at run start = presence-check → reuse-if-fresh → build/refresh (`graphify` / `graphify --update`); **G1 refresh** rides each `integrate` as a code-only (free) `graph-update` sub-step that is **not gated** (a failed refresh is a warning, not a workstream failure); **skip-if-absent** — graphify missing / `--graph off` / oversized repo → **skip silently and continue** (the Caster may record a `recommendations[]` entry), **never auto-installed, never blocks or fails the run**; the graph is **conductor infra — never a workstream (never in `producers[]`), never gated, and NEVER decides a verdict**: any graph-derived gating claim is **verified against live code/tests** (`INFERRED`/`AMBIGUOUS` edges are hints, not facts); `--graph off` disables; default `auto`.

## S35 — graphify-backed audit (`references/audit.md` + `references/gate.md`)
- **Input:** `audit.md` + `gate.md` + an `audit` run with `graphify` present, both `--mode map` and `--mode bugs`.
- **Expected:** the graph is **built once at fan-out start** (loop.md G0) and shared by every specialist; because audit's `integrate` is a no-op there is **no G1**. **`map` = the graph is the substrate**: `GRAPH_REPORT` communities → **module inventory** · god nodes → **key components / entry points** · cross-community bridges + edge density → **dependencies / risk hotspots**, and the synthesizer **folds the `GRAPH_REPORT` in** rather than rebuilding it. **`bugs` = navigation / triage only**: specialists use god nodes / call-paths / blast-radius to *locate* suspects, but a **graph-derived finding is an unconfirmed candidate** — the Reality Checker **reproduces it against live source** before it can be confirmed; a graph-only candidate that can't be reproduced is **dropped** like any other. Absent / `--graph off` / oversized → specialists read the tree directly, the report **still ships** (a `recommendations[]` entry); the graph **never decides a finding's verdict**.

## S36 — recommend the 6 component upstreams (`references/recommend.md` + `references/platforms.md`)
- **Input:** `recommend.md` + `platforms.md`; the Caster hits a gap needing a component beyond the vendored subset (an extra agent / skill / command); plus a non-Claude `--platform` variant.
- **Expected:** all **six upstreams** are surfaced with **real commands carrying no `-y`/auto-confirm**, each labelled by role — **agency-agents** (MIT, *vendored-subset → pull more agents*), **ECC** (MIT, *vendored-subset → extra reviewers/resolvers/skills/commands*), **SuperClaude** (MIT, *vendored-subset → broader `sc:*` suite*), **superpowers** (MIT, ***depended-on* → install the 5 sub-skills + systematic-debugging**), **ui-ux-pro-max** (MIT, ***depended-on* → ux-designer / design roles**), **graphify** (MIT, ***recommend-only*, never vendored**); the Claude-only `/plugin …` form **falls back to git/npx/pip under a non-Claude `--platform`** (the Claude-only form suppressed); these plugin/git/npm/pip sources stay on the **recommend-and-print** path (**not** the one-pinned `npx skills add` auto-`setup` flow). The named **optional reviewer lenses** (silent-failure-hunter · type-design-analyzer · comment-analyzer · code-simplifier/refactor-cleaner — **host-present only, name-don't-vendor**) and the **ECC recommend-targets** (search-first · skill-stocktake · context-budget — **awareness only**) are surfaced as recommend-only. The Caster **recommends, never installs**.

## S37 — learnings project_key + numeric confidence (`references/retro.md` + `references/learnings.md` + `references/caster.md` step 0)
- **Input:** `retro.md` + `learnings.md` + `caster.md`; (a) a retro emitting a record on a repo **with** a git remote; (b) a repo with **no** remote / a deliberate cross-project insight; (c) the Caster consulting a `learnings.md` seeded with one **global** entry + one **project** entry from a *different* project + one from the **current** project.
- **Expected:** the retro stamps each record with **`project_key`** = a stable hash of the repo's **git-remote (origin) URL**, and a **numeric `confidence` 0.3–0.9** (replacing `low|med|high`) — start **0.3–0.5**, **+ per corroborating run up to cap 0.9** (never 1.0), **− per contradicting run / staleness down to floor 0.3**, **below 0.3 dropped, not stored**; a repo with no remote (or a deliberate cross-project insight) is stamped **`global`**; the store gains a `project_key` column. The Caster (precedence **step 0**) consults **GLOBAL entries (`project_key` = `global`, or a blank/legacy cell) always + PROJECT entries whose `project_key` == the current repo's git-remote hash** — the *different*-project entry **never leaks in**; a blank/legacy cell is treated as **global (back-compat)**; matched entries are **overridable defaults weighted by their numeric confidence**, never hard rules; `profile` stays the **primary match key** (+ `task_kind` loose-matched), and the **`android → mobile-dev` normalization still matches**; optional human-gated **project→global promotion** at 2+ distinct projects with avg confidence ≥0.8.

## S38 — opt-in dependency installer (`install.sh` + `install.ps1`)
- **Input:** a scratch-`HOME` dry-run of `install.sh` / `install.ps1` with a depended-on dependency missing; cover interactive **`y`**, interactive **`N`**, and **non-interactive**.
- **Expected:** after the dep-check the installer **prints the registered install commands** (superpowers + `find-skills` + `ui-ux-pro-max` — same as README Step 1) and, **only when a required or recommended item is missing AND the shell is interactive**, prompts **`Install the missing dependencies now? [y/N]`** defaulting **`N`**; on **`y`** it runs the registered commands with **no `-y`/auto-confirm flag** (reporting per-command failures without aborting); on **`N`** it prints `skipped (default N)` and continues; **non-interactive → does not prompt** (prints the commands + continues). It is **strictly opt-in — never auto-installs**, never blocks the skill install, and the **bundled agents + `mle-workflow` are never in the missing / prompt set** (they ship with the plugin).

## S39 — marketplace install path (`README.md` Install + `.claude-plugin/marketplace.json` + `install.sh`/`install.ps1`)
- **Input:** the README Install section + `marketplace.json` + the installers; a user installing via the published Claude Code plugin marketplace, plus the manual fallback.
- **Expected:** the published path is **`/plugin marketplace add adnantaufique/dreamteam`** then **`/plugin install dreamteam@dreamteam-marketplace`**; `marketplace.json` declares **`source: "./"`**, so the package ships the whole bundle (`vendor/` + `skills/` + `plugin.json`); the README states it is **available now that the repo is public** and **honestly notes it has not been client-tested yet**, with **`install.sh` / `install.ps1` as the always-works manual fallback** (the manual path mirrors `skills/dreamteam` → `~/.claude/skills/dreamteam` and runs the dep-check); both paths land the **same bundle**.

## S40 — OpenCode platform (native skill discovery + dispatch + execution) (`references/platforms.md` + `scripts/sync-to-opencode.{sh,ps1}` + `SKILL.md`)
- **Input:** `platforms.md` + `SKILL.md` + the `sync-to-opencode` scripts; (a) a dreamteam run on **`--platform opencode`** (or auto-detected via a `~/.config/opencode` marker); (b) a host that has only run the Claude `install.sh`/`install.ps1` (no opencode sync); (c) the same on a host that ran `sync-to-opencode`.
- **Expected:** dreamteam loads as a **native OpenCode Agent Skill**, discovered on-demand via the native **`skill` tool** — **NOT** an AGENTS.md downgrade (OpenCode's `SKILL.md` format is Claude-compatible; `name` + `description` frontmatter is exactly its required schema). (b) Because OpenCode natively reads **`~/.claude/skills/<name>/SKILL.md`**, the skill is **auto-discovered with no sync step**; (c) `sync-to-opencode.{sh,ps1}` mirrors `SKILL.md` + `references/` into **`~/.config/opencode/skills/dreamteam/`** (the loader injects `SKILL.md` only; `references/*.md` are read on demand via the `read` tool — same on-disk mechanism as the other non-Claude syncs). Dispatch resolves to an **OpenCode subagent via the `task` tool / `@<agent>`** (frontmatter `model: <provider>/<id>`, or JSON under `agent` in `opencode.json`); tiers are **provider-agnostic / BYO-provider — never pinned** (cheap = the provider's mini / native `small_model`; max = its top reasoning model). Execution is the **background bucket — no Workflow tool** (so `--execution workflow --platform opencode` is invalid → note + background fallback), with the honest caveat that OpenCode's `task` dispatch is **synchronous/modal in core** (true non-blocking background only via a community plugin), yet dispatch→await→gate→integrate is unchanged. Global-instructions fallback is `~/.config/opencode/AGENTS.md` (or the shared root `AGENTS.md`, read natively); **web-search is a native Exa-powered `websearch` tool, off by default (needs the OpenCode provider or `OPENCODE_ENABLE_EXA`)** while `webfetch` is native. Auto-detect uses the **`~/.config/opencode` marker, checked before the `~/.agents`→codex fallback**. The 4 existing platforms + the loop/gate/spine contract are **unchanged**.

> S1–S25 are re-run as regression — S26–S40 are additive (S26–S39 verify the Phase A–H bundle build; **S40 verifies the OpenCode 5th-platform wiring**). S37 is the dedicated check for the `project_key` + numeric-confidence learnings schema that S8/S9/S22 also rely on.

## S41 — emit-time evidence (`references/gate.md` §3 + `references/caster.md` dispatch brief)
- **Input:** `gate.md` + `caster.md`; a reviewer that raises a **High** "possible null-deref on the callback path" but cites **no `file:line`, no failing command + output, no reproducing case**; plus a variant where the same finding quotes the verbatim `file:line`.
- **Expected:** the unevidenced finding is **unverified at emit time** — it **does not count**, is **suppressed or flagged `unverified`**, and is **never a must-fix** until re-emitted carrying evidence; the evidenced variant counts normally. This **emit-time admissibility** (§3) is **distinct from §2 reconciliation** (where an *admitted* prediction loses to another reviewer's refuting evidence): §3 decides whether a finding is *admissible*, §2 weighs *admitted* findings. **Low confidence ≠ no evidence** — an evidenced-but-uncertain finding still counts (merely ranked lower, §1–2); only a finding with **no** motivating evidence is barred. The rule **rides the dispatch brief**, so it binds whatever reviewer is cast (vendored or host), the body staying pristine.

## S42 — confidence display-gating never hides a real finding (`references/gate.md` §1–2)
- **Input:** `gate.md` + a panel returning (a) a **High, high-confidence** evidenced bug, (b) a **High, low-confidence** evidenced finding, and (c) a **Low, high-confidence** evidenced note.
- **Expected:** findings carry a **confidence** (certainty) **orthogonal to severity** (impact); synthesis **ranks by confidence within a severity band** so the highest-signal surface first — but the **guard holds: confidence governs display order/emphasis only, never admissibility or must-fix status.** The **High low-confidence** finding **still surfaces** (a high-severity finding always surfaces regardless of confidence) and **must-fix promotion stays by severity alone**; the always-on Reality Checker **always reports**; "zero noise" means **ranking a low-signal note down, never hiding a genuine issue**. A run that drops or buries the High low-confidence finding **fails** this scenario.

## S43 — User-Challenge pauses even under auto (`SKILL.md` §Autonomy + `references/loop.md` §Report)
- **Input:** `SKILL.md` + `loop.md`; an `--autonomy auto` run where mid-run (a) a **Mechanical** call (a tier fixed by the plan), (b) a **Taste** call (two defensible, reversible library choices), and (c) a **User-Challenge** arise — the gate/models want to **change the user's stated direction** (e.g. swap out the user-named framework).
- **Expected:** under `auto` the conductor **proceeds silently** on Mechanical, **proceeds but notes** the Taste choice (+ the alternative not taken), and **PAUSES on the User-Challenge even though autonomy is `auto`** — presenting the **four-part frame** (user's stated direction · recommendation + why · acknowledged blind-spots · cost-if-wrong) and **defaulting to the user**. The taxonomy **composes with, never relaxes,** `auto|confirm|step` (an explicit `confirm`/`step` still pauses as stated).

## S44 — stateless decision log in the report (`references/loop.md` §Report + `SKILL.md` §Autonomy)
- **Input:** `loop.md` + `SKILL.md`; a finished multi-workstream run that made roster/tier/scope picks, one Taste call, and one resolved User-Challenge; plus a variant with **no** non-Mechanical decision.
- **Expected:** the run report **carries a decision-log table** (`# · phase/workstream · decision · class · rationale · rejected alternative`) recording the Taste call, the User-Challenge + its resolution, and any noteworthy Mechanical one — **followed by the handoff notes that close the user-facing report**; it is **report output only — NOT a persisted/event-sourced store, NOT a JSONL/decision file, NOT new state** (dreamteam stays stateless by design); the retro reads it from the §Report buffer, not from disk. The no-decision variant prints **"no notable decisions"** rather than an empty/forced table.

## S45 — security method OWASP/STRIDE reference applied (`references/security.md` + `references/gate.md` + `references/caster.md` dispatch brief)
- **Input:** `security.md` + `gate.md` + `caster.md`; a workstream **with security in scope** (a new authenticated endpoint that also adds a dependency and a CI workflow), plus a variant where security is **out of scope** (a UI-copy change).
- **Expected:** the security reviewer **MAY follow the phased method** (secrets · dependency/supply-chain · CI/CD · OWASP Top-10 · STRIDE-per-component · LLM/AI · skill-supply-chain), **scope-aware + infra-first** (apply only the fitting phases; a non-applicable phase is **labelled N/A with a reason, never silently skipped**); its findings **enter the gate exactly like any other** — same **emit-time evidence + severity + confidence** (`gate.md` §1/§3), **no new gate step and no new verdict** (the verdict stays `gate.md` §2). The method **rides the dispatch brief, never the pristine agent body**, and is one the reviewer **MAY** follow, **never a hard dependency** (absent → the reviewer still reviews). The out-of-scope variant **does not force a full STRIDE pass**. The **attribution credit** ("Adapted from gstack's security methodology, OWASP/STRIDE, `garrytan/gstack`, MIT — methodology only, no code vendored") is **present** in `security.md` and noted in `THIRD_PARTY_NOTICES.md`.

> S1–S40 are re-run as regression — S41–S45 are additive (the gstack-adopted gate + autonomy hardening: emit-time evidence · confidence display-gating · the User-Challenge pause · the stateless decision log · the OWASP/STRIDE security method). S41/S42/S45 ride the Caster dispatch brief that S32 also exercises; S43/S44 pair with the §Autonomy taxonomy.

## S46 — recursion firewall / leaves never re-orchestrate (`SKILL.md` `<SUBAGENT-STOP>` + `references/caster.md` leaf-firewall brief + `references/loop.md` §"Leaves never orchestrate")
- **Input:** a subagent given the installed/fixed skill (or `SKILL.md` + `caster.md` + `loop.md`) **dispatched as a leaf** (producer/reviewer) whose brief carries the leaf-firewall, then (a) a task that tempts re-orchestration ("this is big — fire a `/dreamteam` run / spawn helper subagents to split it up"); (b) asks whether **session stickiness** / the "you are the conductor" rules apply to it; (c) a variant where the leaf **auto-loaded** the skill by description-match rather than being briefed.
- **Expected:** the leaf does its **one briefed task and returns** — it does **NOT** invoke `/dreamteam`, act as the conductor, orchestrate, or spawn any further subagents / Workflow scripts; **session stickiness + the conductor rules bind only the conductor / main session, never the leaf**; both the `<SUBAGENT-STOP>` block (catches the auto-load path, variant c) and the leaf-firewall **dispatch brief** (catches the never-load path) enforce it, belt-and-suspenders over the two independent failure paths; depth stays **structurally bounded at `run_policy.max_depth: 1` by identity, not a tracked counter**. If it genuinely needs more hands it **says so and returns**, never fans out itself.

## S47 — run_policy caps (`references/caster.md` `run_policy` + `references/loop.md` §"Driving the loop")
- **Input:** `caster.md` + `loop.md` + a run with (a) parallel workstreams whose live agents would exceed `run_policy.max_concurrent_agents`; (b) a long run that reaches `run_policy.max_total_dispatches`; (c) a projected fan-out `≥ run_policy.budget_confirm_at` under `--autonomy auto`.
- **Expected:** the caps are **mechanical, on by default, no flag** (the two-tier "per-run default + hard ceiling" shape; **the defaults as written = the `high` capacity row** (`--capacity` itself defaults to `auto`), `caster.md` §"Capacity profile"): (a) concurrency is capped (default **8**, hard ceiling **16** nothing overrides) and the conductor **serializes the excess rather than fanning wider** (the run-wide analogue of `audit_policy.max_parallel_reviewers`); (b) on reaching `max_total_dispatches` (default **60**, counting producers + reviewers + fix **and recovery** re-dispatches + refuters + Caster + setup + retro) the conductor **STOPS and escalates to the human — never silently continues**, tracked as a light integer in the **stateless §Report buffer**, not a persisted store; (c) a projected fan-out `≥ budget_confirm_at` (default **30**, or `--depth exhaustive` / `--evolve`) **prints the projected fan-out + cost and waits for the user's OK even under `auto`**, reusing the existing `confirm` machinery (no new gate). `max_depth` stays **1** (the firewall guarantees it, S46).

## S48 — execution discipline (`references/loop.md` §"Execution discipline" + `references/caster.md` dispatch brief)
- **Input:** `loop.md` + `caster.md` + a producer/reviewer dispatched to run shell commands: (a) a normal command; (b) a long-running / backgrounded command; (c) a command that hangs past the bound.
- **Expected:** (a) the unit **watches the command to completion and READS its output + exit status before proceeding** — never proceeds on an unobserved / unfinished command (the operational half of the emit-time evidence rule, `gate.md` §3: **no observed output = no evidence = not done**); (b) a backgrounded command gets a **bounded** wait/poll (a timeout) then a result check — **never an open-ended wait**; (c) on a hang past the bound it **kills + retries ONCE, or reports the hang and continues with the evidence in hand — never abandons the task sitting in a wait state**; a workstream isn't "done" until its command evidence is actually captured. It's a **discipline** rule (model-compliance, like the leaf firewall) — the harness owns the actual shell/poll, this binds the model's obligation to observe it; it **rides the dispatch brief, never the pristine agent body**, so it binds whatever unit is cast (vendored or host) on all six CLIs.

> S1–S45 are re-run as regression — S46–S48 are additive (the run-level safety guardrails: the recursion firewall / depth-1 leaf invariant · the `run_policy` concurrency + total-dispatch + budget caps · producer/reviewer execution discipline). S46 pairs with the `<SUBAGENT-STOP>` block + the leaf-firewall dispatch brief; S47/S48 ride `run_policy` + the loop's driving rules.

## S49 — dispatch-failure recovery (`references/loop.md` §"Dispatch-failure recovery" + `references/gate.md` §2)
- **Input:** `loop.md` + `gate.md` + `caster.md` + a **Workflow-mode** gate where (a) one reviewer **drops at the structured-output/schema retry cap**, and (b) a parallel stage is **half-failed by a provider rate limit** (some agents returned, some dropped).
- **Expected:** the dropped unit is re-dispatched **ONCE, at the SAME tier, with the SAME brief** — an **infrastructure error is not a reasoning failure**, so **no tier bump** (distinct from §"Model tier + escalation", which reserves ONE-TIER-UP for needs-work/BLOCKED; this is the path that makes "a reviewer that errors is replaced" concrete); the schema-capped reviewer is re-run **schema-free** (plain-text findings + verdict the conductor parses — the schema is a convenience, never worth losing a reviewer over); the half-failed stage is **resumed via the Workflow tool's `resumeFromRunId`** (completed agents replay from cache, only the dropped ones re-run) rather than re-run fresh; the dropped reviewer **never counts as a pass** and its gate slot is **never skipped** — recovered or escalated; **every re-dispatch counts toward `run_policy.max_total_dispatches`** (the same stateless §Report-buffer tally as S47 — no persisted state).

## S50 — verdict admissibility (`references/gate.md` §"Verdict admissibility" + §1/§3)
- **Input:** `gate.md` + a panel where reviewer A returns the literal filler **"Test."** carrying one **evidence-less finding**, reviewer B returns a bare **"LGTM"**, and reviewer C returns a normal evidenced review.
- **Expected:** A and B are **inadmissible** — a placeholder/empty/generic response that doesn't **show its work** (what was actually checked: files read, commands run, §5 phases — plus ≥1 evidenced finding or an explicit pass rationale citing the evidence inspected) is a **REVIEWER ERROR, not a verdict** → each is re-dispatched **once** per `loop.md` §"Dispatch-failure recovery", and if still inadmissible the reviewer is **replaced or escalated** (existing resilience); **neither counts toward `min_pass`** (neither as a pass nor a fail) and A's evidence-less finding **never becomes a must-fix**; C counts normally. This is the **verdict-level counterpart** of §3's per-finding emit-time rule (S41) — **distinct from but complementary to it**: §3 bars an evidence-less *finding*, this bars an evidence-less *review*.

> S1–S48 are re-run as regression — S49–S50 are additive (gate resilience: once-only same-tier re-dispatch on infrastructure error · schema-free re-run · resume-over-rerun · verdict admissibility / placeholder-response rejection). S49 rides the `run_policy` dispatch tally (S47) and stays distinct from the tier-escalation path (S7); S50 pairs with the §3 emit-time evidence rule (S41) and the no-glazing standard (S32).

## S51 — risk-proportional gate depth (`references/caster.md` `gate_policy.risk_scaling` + `references/gate.md` §1–2 + `references/loop.md`)
- **Input:** `caster.md` + `gate.md` + `loop.md` + a run whose manifest carries a 3-reviewer panel (Code Reviewer, Security Engineer, Reality Checker) and whose plan has three workstreams the conductor classifies **low / standard / high**, plus one workstream **touching auth** (security in scope); plus a **ux-designer-profile** run (the a11y-covering reviewer on its panel) with a **standard**-class workstream; variants: (a) `risk_scaling: off`, (b) `--cost quality`, (c) an explicit `--roster reviewers=…`, (d) `--full-gate`.
- **Expected:** the gate dispatches the **risk-scaled subset** per class — low → **Reality Checker only**; standard → **Reality Checker + the most relevant domain reviewer**; high → **the full panel**; **Reality Checker is ALWAYS dispatched and its pass ALWAYS required** whatever the class; the auth workstream is classed **`high` by the sensitive-surface heuristic** (security in scope is never a lower class) → the full panel, Security Engineer included; on the ux-designer run's **standard** workstream the **a11y-covering reviewer is force-included IN ADDITION** to the class's base subset (a11y is a standing profile-level non-waivable dimension, `gate.md` §3, so standard+a11y is a legal state — classes subtract, guards force-include, belt-and-suspenders against **misclassification**: the force-include holds even when the conductor classed the workstream too low); classification follows the stated heuristics (diff scope · sensitive surface · novelty · claim exposure) with **any doubt → the higher class**; `min_pass` counts **of the dispatched subset** (an explicit numeric `min_pass` is a **floor** — the subset widens to satisfy it); the re-anchor line **prints the class** ("Dispatching `<role>` for WS-N at `<tier>`, risk `<class>` — …"); the (a) off / (b) quality / (c) explicit-roster / (d) `--full-gate` variants all get the **full panel on every workstream**, their re-anchor lines printing **risk `off`**; an RC-only `low` panel **can never trigger the devil's-advocate pass at the defaults** (size 1 sits below the default `devils_advocate_min_panel: 3`); the `audit` profile's reviewer fan-out (`audit_policy`, `references/audit.md`) is **untouched** — risk scaling governs the produce→gate→fix loop's gate, never an audit run's fan-out, and it changes **panel size, never tiers** (reviewers stay capable).

## S52 — effort dial (`references/platforms.md` + `references/caster.md` §"Model tier")
- **Input:** `platforms.md` + `caster.md` + a **cheap-tier mechanical producer** and a **Reality Checker** dispatched (a) on Codex and (b) on Claude Code **Workflow mode**; variant (c): the same dispatches in **background-subagent mode** (the Agent tool).
- **Expected:** where the platform exposes an effort dial (Codex `model_reasoning_effort`; Claude Code **Workflow-mode `agent()`**'s `effort` option) the **cheap-tier mechanical producer MAY run at low effort**; **reviewers — Reality Checker especially — never run below the platform default**; the dial **complements the tier rubric, never substitutes for a tier drop** (each role's tier still comes from the rubric/manifest); in variant (c) the Agent tool exposes **no effort lever** — the dispatch simply carries none, which is **fine, not an error** (never a reason to skip, downgrade, or re-route the dispatch).

> S1–S50 are re-run as regression — S51–S52 are additive (cost-proportional gating: risk-scaled reviewer subsets with force-included guards · the effort dial complementing the tier rubric). S51 rides the min_pass + devil's-advocate machinery (S21) and the non-waivable dimensions (S29/S45) without changing the verdict logic; S52 pairs with the tier rubric (S6) and cross-CLI resolution (S11).

## S53 — pre-fix refuter pass (`references/gate.md` §4 + §2 + `references/caster.md`)
- **Input:** `gate.md` + `caster.md` + a gate whose synthesis leaves **three must-fixes**: (a) one carrying a **failing test + its output** (hard run evidence); (b) a **prediction** (reasoning + verbatim `file:line`, no run evidence) that a **green targeted run refutes**; (c) a **prediction the refuter cannot settle** (no evidence either way); plus a **needs-work** variant and a **second-fix-iteration** variant.
- **Expected:** the evidenced must-fix (a) gets **NO refuter** (already proven — nothing to refute); (b)'s refuter produces the green run → the prediction is **dropped with the refutation noted, per §2 evidence-beats-prediction** (the refuter **reuses §2's existing reconciliation — no new verdict machinery**); (c) **STANDS** (guilty-until-evidenced unchanged — the refuter kills a finding **only** with hard refuting evidence, never by opinion, and the split is by **evidence kind, never confidence**); refuters fire **once per gate, before fix iteration 1 only** — **never on a needs-work** (that escalates, it never enters the fix loop) and **never inside later iterations** (the second-iteration variant dispatches none); **one refuter per predicted must-fix, parallel across findings**, each charged to run the test/build/repro the prediction implies; the refuter is an **evidence-producer at `standard` tier — NOT a gate reviewer, NOT a verdict-holder** — so the **"Reviewers never drop below capable" floor stays literally true**; one targeted refuter dispatch is cheaper than a fix round burned on a false positive; every refuter dispatch **counts toward `run_policy.max_total_dispatches`**.

## S54 — dispatch-reliability record (`references/loop.md` §Report + `references/retro.md`)
- **Input:** `loop.md` + `retro.md` + a finished run in which one reviewer **dropped at the structured-output/schema cap** and was re-run **schema-free**, one **placeholder verdict** ("Test.") earned a recovery re-dispatch, and one predicted must-fix was **refuted by its refuter**.
- **Expected:** the **§Report buffer carries a per-role dispatch-reliability tally** — drops **by failure class** (**dropped-without-returning** vs **returned-but-inadmissible**), recovery re-dispatches, schema-cap **schema-free re-runs**, and **refuter outcomes** (**refuted-and-dropped** vs **stood**); the retro's **feedback reviewer can cite those counts as run evidence for a learning** (a role that repeatedly drops, or a reviewer whose predictions are repeatedly refuted, is itself an evidence-backed learning); it is **report output only — NOT a persisted store, NOT a JSONL file, no new schema — dreamteam stays stateless**.

> S1–S52 are re-run as regression — S53–S54 are additive (accuracy: the pre-fix refuter settling predicted blockers before the first fix iteration · the per-role dispatch-reliability record in the stateless report). S53 rides §2's evidence-beats-prediction reconciliation (S3) and stays outside both the fix-loop cap (S7) and the devil's-advocate pass (S21); S54 pairs with the stateless decision log (S44) and the recovery/admissibility machinery (S49/S50).

## S55 — delta-scoped re-verification (`references/gate.md` §4 + §2)
- **Input:** `gate.md` + a **fix-then-pass** gate entering the fix loop: in iteration 1 the producer's fix stays **within the original workstream's diff**; a variant where the fix **also edits a file OUTSIDE that diff**; and a **second-iteration variant** where, after the expansion's fresh review lands, a later fix **re-touches that previously-expanded file**.
- **Expected:** for the in-scope fix, re-verification is **scoped to (a) confirming each must-fix is actually resolved and (b) the fix's own diff for regressions** — **never a fresh full review of the whole workstream** (a fix round re-reviews the delta, not the world); in the out-of-scope variant the **expansion is fresh, unreviewed surface** → the conductor **re-classifies over the expanded surface** (the `caster.md` heuristics re-applied: sensitive surface → high, any doubt → the higher class, non-waivable force-includes fire) and the expansion gets a **FRESH REVIEW by the panel of its re-derived risk class** (the risk-scaled subset, §1 / `references/caster.md`), **never mere re-verification** — a dispatch-time class is never frozen, and scoping narrows the re-check of already-reviewed ground, never the review of new ground; in the **second-iteration variant** the re-touched file is **already-reviewed ground — once its fresh review landed, the expansion's files JOINED the workstream's reviewed diff** — so the later fix gets **DELTA re-verification, never a second fresh review** (never fresh surface twice); **§2's re-synthesis is unchanged** (must-fix confirmation stays mandatory, the verdict still comes from §2) and the gate is never weakened — an expansion gets **more** review, not less.

## S56 — cache-stable dispatch-brief assembly (`references/caster.md` §"Dispatch-brief assembly")
- **Input:** `caster.md` + two dispatches in one run: (a) a **producer** and (b) a **security-cast reviewer**.
- **Expected:** both briefs open with the **byte-identical preamble of standing briefs in the one fixed order, firewall-first** — leaf firewall (deliberately leading: "placed first", `SKILL.md` §Resilience) → no-glazing → evidence → confidence → security (conditional: it occupies its fixed slot **only when a security-covering reviewer is cast**, as here; else the slot is simply absent) → execution discipline → report-what-you-checked — with **all per-leaf content** (role, task, success criterion, working set, context) in a **tail strictly after the preamble**; **no standing rule's content is altered** — each block keeps its exact text and its own stated scope (a reviewer-scoped rule binds only reviewers wherever it rides) — this is an **ordering/stability rule only**, and the byte-identical claim is **scoped to post-cast dispatches while the manifest's reviewer set is unchanged** (the Caster's pre-manifest dispatch sits outside it; a re-cast starts a new stable prefix — cost-only, never correctness); the stated benefit: **provider-side prompt caching hits the shared prefix** across those dispatches where supported, and the **ordering rule costs nothing** on a platform without prefix caching.

## S57 — working-set handoff (`references/loop.md` dispatch bullets + §Graph)
- **Input:** `loop.md` + three dispatches: (a) a unit whose brief carries **file paths, entry points, and graph pointers** (graph on); (b) a leaf whose set proves **insufficient mid-task** and that steps outside it; (c) a leaf whose set is **fundamentally wrong** for the task.
- **Expected:** the dispatch brief **hands each unit its working set** — the relevant file paths, entry points, and (when the graph is on, §Graph) graph pointers — so the leaf spends its budget on the task, **not on re-mapping the repo** (the conductor-side counterpart of §Graph's shared-map rationale); leaf (b) MAY explore beyond the set **and says so in its return** — that is reporting, never orchestrating: the leaf firewall is untouched; leaf (c)'s genuinely insufficient set escalates as the **existing NEEDS_CONTEXT** (add the missing context, **re-dispatch SAME tier** — no tier bump, no new status).

> S1–S54 are re-run as regression — S55–S57 are additive (dispatch efficiency: delta-scoped re-verification in the fix loop · the cache-stable fixed-order brief preamble · the working-set handoff on dispatch). S55 rides §2's re-synthesis (S3) and the risk-class panel (S51) — expansions get more review, never less; S56 changes ordering only — the standing briefs S32/S41/S42/S45/S46/S48/S50 ride on are byte-unchanged; S57 composes with NEEDS_CONTEXT (S7/S17) and the leaf firewall (S46).

## S58 — budget-aware scaling (`references/caster.md` `run_policy` + `references/loop.md` §"Driving the loop" + `references/platforms.md`)
- **Input:** `caster.md` + `loop.md` + `platforms.md` + three runs: (a) a **Workflow-mode** run on Claude Code carrying a **user token target** that comes under **budget pressure** mid-run; (b) a **background-mode** run on the same CLI; (c) a **Codex** run — (b) and (c) **also given a user token target**.
- **Expected:** (a) the conductor honors the Workflow harness's **live remaining-token budget as a run ceiling alongside the static caps** — the §Autonomy **budget-print/projection incorporates it** — and under pressure **degrades by SCHEDULING ONLY, in the stated order**: **serialize parallel workstreams (less concurrency) → defer or shrink the remaining fan-out (fewer/smaller workstreams, postponed optional dispatches — "optional" = a dispatch whose absence cannot change a verdict: a skipped pre-fix refuter is fail-safe (no evidence → the finding STANDS, `gate.md` §4), while an ARMED devil's-advocate pass (`devils_advocate_on_unanimous: true`, e.g. the `audit` profile) is part of the gate and never skipped or postponed; deferrals reported, never a silent scope drop) → STOP + escalate to the human, exactly like reaching `max_total_dispatches`**; under **maximum** pressure the **never-weaken guarantee holds** — **no risk reclassification, no panel below its class, no dropped Reality Checker or non-waivable-dimension reviewer, no tier-floor or honesty-rule relaxation** — a run that **cannot afford its gates STOPS**; it does **not** ship less-reviewed work; (b)/(c) the **background** run and the **Codex** run use the **static caps as the only layer** (no harness budget exists on the background path or the other CLIs, `references/platforms.md`) — and that is **correct behavior, not an error**: their token target is **ACKNOWLEDGED explicitly (no live budget on this path; the static caps govern) — never silently ignored**, and the conductor does **not** hand-track tokens itself.

> S1–S57 are re-run as regression — S58 is additive (budget-aware scaling: the Workflow harness's live token budget honored as a run ceiling by scheduling only — the gate never weakens, and the static caps stay the floor and the only layer off-Workflow). S58 rides the `run_policy` caps (S47) and the execution-mode split (S23/S25); its never-weaken guarantee pins the risk-class guards (S51), the reviewer tier floor (S6), and the honesty rules (S32/S41) fixed under budget pressure.

## S59 — agent scouting ledger (`references/learnings.md` + `references/retro.md` + `references/caster.md` step 0)
- **Input:** `learnings.md` + `retro.md` + `caster.md`; a ledger seeded with producer agent **X** at `2 dispatches · 2 first-try passes` and a same-role alternative **Z** at `2 dispatches · 1 first-try pass` (both current-project rows); a **finished run** whose run record (`loop.md` §Report buffer, including the dispatch-reliability tally) shows X **first-try-passed** its workstream and reviewer agent **Y** had **2 refuted predictions**; then a **next-run cast** on the same project where X and Z both fit the same producer role; variant: (a) the same cast with an explicit `--roster` naming Z.
- **Expected:** the retro **appends/updates the ledger rows** in `learnings.md` from the run's evidence only — X's matching row **increments** to `3 dispatches · 3 first-try passes` (same agent + `project_key` + `profile` → increment, never a duplicate row; `profile` alone is the match key — the cell's task_kind tail is a descriptive annotation, never a match criterion), Y's row gains **+2 refuted predictions** (refuter outcomes are **already keyed to the emitting reviewer**, `loop.md` §Report — no re-derivation), and **no row is written without run evidence** (the store's honesty rule); the next cast **prefers X between the fitting candidates and its rationale line cites the numbers** (e.g. "ledger: 3/3 first-try; Z's 1/2 sits below the noise floor"); **Z's 2-dispatch row is ignored as noise** (fewer than 3 dispatches — low-sample humility: a thin record is *no* signal, not a bad record); (a) the explicit **`--roster` naming Z is honored unchanged** — the ledger never overrides an explicit cast; the **Reality Checker is never affected by any record** (always-on regardless); rows are **advisory defaults, never bans** (Y's refuted predictions surface as rationale, never exclude Y outright); `project_key` scoping is **identical to the learnings table** (global + current-project rows only); the run itself stays **stateless** — the ledger is written **only by the retro**, into `references/learnings.md`, the already-sanctioned persistence point (the run record itself remains report output, never a store).

> S1–S58 are re-run as regression — S59 is additive (agent scouting ledger: the retro accumulates per-agent reliability from the run record (`loop.md` §Report buffer, including the dispatch-reliability tally) into `learnings.md`; the Caster consults it as an advisory tie-break between fitting candidates). S59 rides the reliability record (S54) and the learnings `project_key` scoping (S37); the cast precedence, `--roster` override, fast-path rule, and always-on Reality Checker are unchanged.

## S60 — plain-language guidance layer (`references/loop.md` §Report + `SKILL.md` §Resilience + `references/gate.md` §2)
- **Input:** `loop.md` + `SKILL.md` + `gate.md` + (a) a **finished 3-workstream run** whose gate verdicts are **pass / fix-then-pass (2 must-fixes fixed) / needs-work**; (b) a run that **STOPs at `run_policy.max_total_dispatches`**; (c) an **empty-handoff variant** — every workstream verified, nothing left for a human.
- **Expected:** (a) each workstream's report carries, **directly after its verdict elements, ONE plain-language sentence** stating what the gate checked and what that means — **faithful to `gate.md` §2's definitions**: the pass reads like "an independent reviewer re-ran the tests and confirmed the claims — safe to build on"; the **fix-then-pass names the real problems found and that they were fixed and re-checked — NEVER rounded up to sound like a clean pass** (the no-glazing standard, S32, applies to the gloss too); the needs-work states the **approach itself failed review, nothing was accepted into the project, and a human decision is needed**; one sentence each, never a paragraph. The run **closes the user-facing report with handoff notes** — up to 5 plain bullets naming the human follow-ups: what to review manually (user-facing copy, visual checks), what the gate labeled **manual-only/unverified** per `gate.md` §3's proof-labeling (never upgraded), mid-run debt/deferrals, and security/config steps the run couldn't do (secrets, deploys); (c) the empty variant prints the explicit **"nothing outstanding — all workstreams verified"** line rather than omitting the section. (b) the cap-STOP escalation presents the **four-part options frame** (`SKILL.md` §Resilience — the §Autonomy frame's shape, not a new mechanism): what happened and why the run stopped · the recommendation · **2–3 concrete options each with its cost** (e.g. raise the cap and continue / split the task / take the verified partial result and stop) · **a stated default if the user just says "continue"** — an option may RAISE a cap, **never lower the gate**. All of it is **additive**: the expert verdict elements (verdict · evidence · tier path · decision log · reliability record) are **unchanged and remain the report of record**; **default on, no flag, no persisted state** (printed report only).

> S1–S59 are re-run as regression — S60 is additive (plain-language guidance: the one-sentence verdict gloss faithful to `gate.md` §2 · end-of-run handoff notes naming the human follow-ups, with the explicit nothing-outstanding empty case · the four-part options frame on a cap-STOP). S60 rides the no-glazing standard (S32), the honesty-labeling rules (S33/S41), the User-Challenge frame (S43), and the `run_policy` STOP (S47) — no existing report element changes.

## S61 — cast-time MCP awareness (`references/caster.md` + `references/loop.md` + `references/platforms.md`)
- **Input:** `caster.md` + `loop.md` + `platforms.md` + two hosts: (a) one whose MCP config carries a **connected browser-automation server** (e.g. Playwright); (b) one with **no MCP servers connected**; on each, a cast whose panel includes a **verifying reviewer**.
- **Expected:** at cast time the conductor **inventories the relevant connected MCP capability as CAPABILITY CLASSES** (browser automation, docs lookup, db access — never an exhaustive tool dump), resolved **per the host's own MCP config, never assumed** (`platforms.md` — MCP servers are host-level, on all six CLIs); on host (a) the verifying reviewer's **working set names the available capability** ("browser automation available via the connected Playwright MCP server") — per-unit working-set content in the brief's **TAIL**, never a new standing preamble brief (S56's fixed order untouched); on host (b) the inventory **skips silently** — the unit **works natively**, with **no block and no required `recommendations[]` entry** (the graphify skip-if-absent posture); **availability only, never a dependency**: nothing is installed, no gate requires an MCP tool, availability never enters a verdict or `min_pass`, and a unit is never failed for lacking a server.

## S62 — skill-usage feedback (`references/loop.md` §"Model tier + escalation" + `references/retro.md` + `references/learnings.md`)
- **Input:** `loop.md` + `retro.md` + a finished run in which producer **P** had `ponytail` in its manifest `skills[]` and **never used it**, and producer **Q** cited `karpathy-guidelines` as **shaping its diff**; then the next cast on the same project.
- **Expected:** both returns carry the **ONE-line skill-usage element** — P's reads "attached skills unused", Q's names `karpathy-guidelines` and, briefly, its effect — **one return element, never a new schema or status**; the **run record carries it** to the retro; the retro **MAY emit an evidence-backed learnings row in the LEARNINGS table — never the agent scouting ledger** (usage advice is prose, not counts), e.g. *"task_kind X: `ponytail` attached but unused across 3 runs"* or *"`karpathy-guidelines` cited as shaping the diff"*, under the **same evidence rule and the same numeric-`confidence` mechanics** as any learning; the next cast's **step-0 consult may surface it** as an overridable attachment default; **advice, never enforcement — a learning never auto-detaches a skill**, and an explicit `--skills`/`--roster` is honored unchanged.

> S1–S60 are re-run as regression — S61–S62 are additive (cast-time awareness: the conductor's MCP capability inventory riding the working set, availability-only · the producers' one-line skill-usage feedback feeding the retro's attachment learnings). S61 rides the working-set handoff (S57), the graphify skip-if-absent posture (S34), and the per-host platform resolution (S11) — no gate, verdict, or manifest field changes; S62 rides the learnings store + evidence rule (S8/S37) and the run-record machinery (S54), and composes with the step-0 consult (S9/S59) — attachments stay the Caster's call, never auto-detached.

## S63 — capacity profile (`references/caster.md` §"Capacity profile" + `references/loop.md` + `SKILL.md` + `references/platforms.md`)
- **Input:** `caster.md` + `loop.md` + `SKILL.md` + `platforms.md` + three runs: (a) a **below-capable conducting model** on a plan where **capable-tier dispatch is unavailable**, no flag; (b) the **same run with `--capacity high` explicit**; (c) a **capable conductor**, no flag; plus a variant where a **mid-run capable-unavailable event** hits an otherwise-`standard` run.
- **Expected:** **`auto` resolves per the heuristic** — the CONDUCTING model's own tier as the plan proxy, stated as a **proxy (overridable), never an oracle**: (a) → **`low`**, (c) → **`standard`**; (a)'s `low` remaps the `run_policy` per-run defaults to **`max_concurrent_agents` 3 · `max_total_dispatches` 30 · `budget_confirm_at` 15**, biases `workstream_strategy` **`sequential`**, and resolves the reviewer floor to the **BEST AVAILABLE tier with the substitution declared ONCE at cast time as the capacity note in the manifest `rationale` — NO per-dispatch tier-unavailable nagging** (the per-dispatch flag remains only for a **surprise beyond the declared profile**, `loop.md`); (b) the **explicit flag WINS** over the heuristic → `high` = today's defaults (**8 · 60 · 30**); the mid-run event **downgrades the resolved profile ONCE, flagged in the report — never silently, never per-dispatch flip-flopping**; the **hard ceilings are unchanged** (nothing overrides them); **across all three runs the GATE BAR is IDENTICAL** — the **Reality Checker is always dispatched and its pass always required**, risk-scaling guards + **non-waivable force-includes hold** (no panel below its risk class), **`min_pass` semantics and the evidence/honesty rules unchanged** — capacity remaps **scheduling numbers and tier resolution only, never the gate**; on `low` the conductor **integrates after EVERY workstream** (a session-limit kill never loses a gated result) and recovery after a kill is **resume-over-rerun, never a fresh re-run of gated work** (`loop.md` §"Dispatch-failure recovery"); explicit **`--models`/`--roster`/`--cost` still override per §Precedence** (capacity sets defaults; explicit flags win).

> S1–S62 are re-run as regression — S63 is additive (capacity parity: the plan-sized capacity profile remapping `run_policy` per-run defaults + reviewer-tier resolution off the conducting-model heuristic, the one-time cast-time capacity note replacing per-dispatch tier flagging, and `low`'s integrate-every-workstream + resume-over-rerun discipline — the same gate bar at every plan). S63 rides the `run_policy` caps (S47), the reviewer tier floor (S6) and tier-unavailable fallback (S7's escalation section), and budget-aware scaling's never-weaken family (S58) — capacity is the defaults profile, the live budget the ceiling; both move scheduling only, neither the gate.

## S64 — weak-conductor guard (`SKILL.md` §Autonomy + `references/gate.md` §2 + `references/caster.md` §"Capacity profile")
- **Input:** `SKILL.md` + `gate.md` + `caster.md` + a run on a **below-capable conducting model** — invoked with an explicit **`--capacity high`** — with (i) a workstream whose risk class is **borderline standard-vs-high**; (ii) a gate with **conflicting reviewer verdicts reconcilable by §2 step 3** (one reviewer's prediction, another's hard refuting evidence); (iii) a gate with **conflicting verdicts NOT reconcilable by any §2 step**; (iv) a **fix-then-pass the conductor is tempted to report as a clean pass**; and (v) a **capable-conductor control run** over the same four cases.
- **Expected:** the guard **arms off the MODEL, never the flag** — the below-capable conductor is guarded **even under the explicit `--capacity high`** (the flag sizes scheduling; the guard compensates judgment — the same below-capable signal capacity's `auto` heuristic reads, `caster.md` §"Capacity profile"), **the arming DECLARED once at run start** (a one-line manifest-`rationale` note, e.g. "weak-conductor guard armed (conducting model below capable)" — never silent, so its pauses aren't misread as low capacity); (i) classifies **`high`** — under the guard the "any doubt → the higher class" heuristic is **ABSOLUTE**: a borderline class goes higher, never argued down; (ii) **resolves normally via the quoted step** — the conductor states §2 step 3's outcome (evidence beats prediction) and proceeds: **the guard never pauses what the checklist settles**; (iii) **PAUSES with the four-part frame** — a synthesis no §2 step justifies is **User-Challenge-treated: escalated to the user, never improvised**; (iv) the **no-glazing rule holds** — reported as a **fix-then-pass with the guard's checklist-quoted synthesis** (dedup → prioritize → reconcile → decide, each step's outcome stated), never rounded up to a clean pass; **integration states the `verification-before-completion` evidence and the commit-message self-check outcome explicitly** (`loop.md`) — the checks already exist; under the guard silently "having done them" doesn't count (the verdict-admissibility show-your-work principle at the conductor level); (v) the **control run behaves exactly as today — the guard adds NOTHING on a capable conductor** (a no-op, never a lighter variant); and **the gate bar is IDENTICAL in both runs** — same verdict semantics, `min_pass`, and honesty rules: the guard is **compensatory, never a bar change — only WHO decides borderline cases shifts, from the weak conductor to the user**, at the deliberate price of more user interaction on low capacity.

> S1–S63 are re-run as regression — S64 is additive (weak-conductor guard: a below-capable conductor follows the written checklists verbatim and pauses where it would otherwise guess — borderline classifications go higher absolutely, gate synthesis is quoted step-by-step per `gate.md` §2, an unreconcilable synthesis escalates via the four-part frame, and integration checks are stated explicitly; armed off the conducting model even under `--capacity high`, a no-op on a capable conductor, the gate bar unchanged). S64 rides the capacity-profile signal (S63), the §Autonomy taxonomy + four-part frame (S43), the risk-class doubt rule (S51), §2's synthesis machinery (S3), and the no-glazing standard (S32/S60) — no verdict logic changes.

## S65 — Cursor platform (native skill discovery + dispatch + execution) (`references/platforms.md` + `scripts/sync-to-cursor.{sh,ps1}` + `SKILL.md`)
- **Input:** `platforms.md` + `SKILL.md` + the `sync-to-cursor` scripts; (a) a **`--platform auto`** run on a host with a `~/.cursor` marker, plus an explicit **`--platform cursor`** run; (b) a host that has only run the Claude `install.sh`/`install.ps1` (no cursor sync); (c) the same on a host that ran `sync-to-cursor`.
- **Expected:** (a) auto-detect resolves **cursor** off the **`~/.cursor` marker**, checked (like OpenCode's `~/.config/opencode`) **before the `~/.agents`→codex fallback** — Cursor reads `~/.agents/skills` + `~/.claude/skills` + project `AGENTS.md`, so the distinctive marker must win; the explicit flag overrides the guess. dreamteam loads as a **native Cursor Agent Skill** (the agentskills.io open standard — `name` + `description` frontmatter, Claude-compatible), auto-matched on `description` with `references/` loaded on demand — **NOT** an AGENTS.md downgrade (a project-root `AGENTS.md` is read natively; no *global* AGENTS.md file is documented — global prose preferences are Cursor's UI-managed User Rules). (b) Because Cursor reads **`~/.claude/skills/`** for compatibility, the skill is **auto-discovered with no sync step**; (c) `sync-to-cursor.{sh,ps1}` mirrors `SKILL.md` + `references/` into **`~/.cursor/skills/dreamteam/`**. Dispatch resolves to a **native Cursor subagent** — markdown + YAML frontmatter in `~/.cursor/agents/` (or project `.cursor/agents/`) with `model:` (a concrete picker ID, or `inherit`) and **`is_background: true`** — via the **`Task` tool** (parallel = multiple Task calls in one message), `/<agent>`, or auto-delegation; **background dispatch is native and non-blocking** (no OpenCode-style synchronous caveat applies), and the **depth-1 invariant rests on the prose leaf firewall exactly as on the other non-Claude platforms** — Cursor's native nesting cap binds one level **below** the leaf (since 2.5 a direct subagent **CAN** spawn child subagents; only a subagent's subagent can't spawn further), a bound on runaway depth, **never depth-1 enforcement** — dispatch→await→gate→integrate unchanged. Tiers are **picker-resolved, never pinned** (cheap = the picker's mini/flash rung; max = its top reasoning model, with `-high` reasoning-effort variants as separate model IDs, never a dispatch-time dial; `Auto`/`Premium` are Cursor's own routing modes). Execution is the **background bucket — no Workflow tool** (so `--execution workflow --platform cursor` is invalid → note + background fallback). The 5 existing platforms + the loop/gate/spine contract are **unchanged**.

> S1–S64 are re-run as regression — S65 is additive (Cursor platform: native Agent-Skills discovery including the `~/.claude/skills` compatibility read, Task-tool subagent dispatch with native background + parallel execution and the firewall-held depth-1 invariant (the native nesting cap binds only one level below the leaf), picker-resolved never-pinned tiers, the `~/.cursor` auto-detect marker checked before the `~/.agents`→codex fallback, and the `~/.cursor/skills` sync target).

## S66 — run context package (`references/caster.md` + `references/loop.md` + `references/gate.md`)
- **Input:** `caster.md` + `loop.md` + `gate.md` + a **2-workstream run from an approved plan whose discovery mapped N files**; WS1's producer **touches one file OUTSIDE that map** and returns the delta; then WS1's gate runs and WS2's producer dispatches.
- **Expected:** **ONE run context package is built at run start from the plan's own discovery — the plan carries one, so it IS the source and NO conductor scout fires** (the cheapest-source rule, `caster.md`; the scout is only for a plan that carries none; no per-leaf re-exploration, no second mechanism), carrying **FACTS ONLY** — the affected-file map (paths + one-line roles), entry points, the build/test/verify commands, a conventions pointer, the relevant MCP capability (the cast-time inventory **folded in as one package field**, S61), and graph pointers when the graph is on; **every dispatch brief's TAIL carries it** — producer, reviewer, refuter, fix re-dispatch alike — **each carriage OPENING with the fixed guard clause** (map-not-testimony · verify-everything-yourself · off-map-always-admissible · producer-facts-are-claims — spec'd once in `caster.md`'s carriage format, cited by `gate.md` §1), with the **cache-stable preamble untouched** (the fixed firewall-first order, `caster.md` §"Dispatch-brief assembly" / S56); WS1's producer's return carries the **ONE FACTS-DISCOVERED element** (files actually touched · the exact verify command run · the off-map file as a **"package delta"** — the same one-element pattern as the skill-usage line, never a new schema or status); **WS1's reviewers receive the package + the touched-file list + the verify command — labeled the producer's CLAIMS, never ground truth** (the list is confirmed against the **actual diff** by `gate.md` §5's diff-review; the verify command is a suggestion whose sufficiency the reviewer judges) — **so they VERIFY rather than re-map — and still RE-RUN verification themselves** (`gate.md` §3 unchanged, the §1 independence guard: the package is a **map, never testimony**, and **a reviewer remains free to look beyond the map** — a finding is never inadmissible for being off-map); the conductor **merges the off-map file into the package BETWEEN dispatches — never mid-dispatch — so it is in the package before WS2's producer dispatches**; **no conclusion ever appears in the package** ("correct", "tests pass", "looks good" are banned — facts travel, conclusions never, and no check is removed); and the package is **per-run, in-brief content only — no persisted store, no new state** (statelessness intact).

> S1–S65 are re-run as regression — S66 is additive (run context package: the once-per-run facts-only repo map — file map, entry points, verify commands, conventions pointer, folded-in MCP capability, graph pointers — riding every brief's tail, kept living by the producers' FACTS-DISCOVERED returns, with the gate handoff letting reviewers verify rather than re-map). S66 extends the working-set handoff (S57) into its fuller form and folds the MCP inventory (S61) in as one package field; it rides the cache-stable preamble (S56 — tail-only, the fixed order untouched) and the one-element return pattern (S62), and its independence guard pins `gate.md` §3's evidence-not-trust (S3/S32/S41) unchanged — speed comes only from added facts and deleted duplicated work, never from a removed check.

## Grounding A — Android build (dry-run)
- **Input:** installed skill; `/dreamteam "build an Android TV app from its spec"`. Selection + description only.
- **Expected:** picks the **mobile-dev** profile dreamteam (Mobile App Builder + UI Designer); **sequential** workstreams; gate **[Code Reviewer, Reality Checker]**.

## Grounding B — ML research salvage (dry-run)
- **Input:** installed skill; `/dreamteam --profile ai-research "salvage a failed-hypothesis study: expand new directions ∥ polish current findings for publication"`. Selection + description only.
- **Expected:** picks the **ai-research** dreamteam; **two parallel workstreams** (*expand* ∥ *polish*); gate **[a methodology reviewer, Reality Checker]**.
