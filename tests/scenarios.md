# Dreamteam — Validation Scenarios (subagent tests)

Each scenario is run by dispatching a fresh **`general-purpose`** subagent, giving it the named skill file(s) (or the installed skill) + the input, and asserting its output matches **Expected**. Re-run after any edit. There is no code compiler — *the subagent's behavior is the test*. Where noted, **judge coherence, not exact agent picks**.

Install first so installed-skill scenarios see the latest copy: `pwsh ./install.ps1` (Windows) or `bash ./install.sh` (Linux/macOS) — publishes `skills/dreamteam/` → `~/.claude/skills/dreamteam/`.

## S1 — profile fast-path (`references/profiles.md`)
- **Input:** `profiles.md` + task "build an Android TV app".
- **Expected:** the **mobile-dev** dreamteam — producer Mobile App Builder (+ UI Designer for design), gate **[Code Reviewer, Reality Checker]**, `sequential`. Each role carries its default tier (Mobile App Builder@standard, UI Designer@cheap, reviewers@capable; abstract tiers, resolved per platform).

## S2 — Caster agent, no profile (`references/caster.md`)
- **Input:** `caster.md`; act as the Caster agent for task "harden and load-test our REST API" (no `--profile`/`--roster`).
- **Expected:** a coherent dreamteam manifest — a planner; producer(s) (e.g. Backend Architect / API Tester / Performance Benchmarker / Security Engineer); `reviewers` **including Reality Checker**; `gate_policy`; `workstream_strategy`; one-line rationale per pick. *Judge coherence, not exact picks.*

## S3 — gate synthesis + honesty (`references/gate.md`)
- **Input:** `gate.md` + two mock reviews: (1) Code Reviewer "Approve-with-changes" — a real **High** (resource leak, found by code-read) + a **predicted High** (build break); (2) Reality Checker "PASS" whose green build **refutes** the predicted build break and which did **not** exercise the leak path.
- **Expected:** keeps the leak as the surviving **must-fix**; **drops** the refuted build-break (evidence beats prediction); verdict = **fix-then-pass**.

## S4 — core loop + isolation (`references/loop.md`)
- **Input:** `loop.md` + `gate.md` + a 2-workstream **parallel** plan (both producers mutate the same server file) + a parallel manifest.
- **Expected:** describes **produce→gate→fix→integrate** per workstream; gives **each parallel file-mutating producer its own git worktree**; surfaces **each** workstream's gate verdict; integrates only on pass; needs-work → stop + escalate.

## S5 — end-to-end dry-run (installed `SKILL.md`)
- **Input:** installed skill; invocation `/dreamteam "add OAuth login to our web app" --autonomy confirm`. Dry-run (no execution).
- **Expected:** resolves + prints the **web** dreamteam (+ Security Engineer for auth, + Reality Checker) with rationale; describes the produce→gate→fix→integrate loop; **stops for confirmation, does not execute**.

## S6 — model-tier assignment (`references/caster.md`)
- **Input:** `caster.md` + a mixed task with a clear mechanical sub-task, an integration sub-task, and review — e.g. "add a config constant (1 file) and refactor the auth middleware across the service, then review"; no `--profile`/`--models`.
- **Expected:** the manifest assigns the **mechanical role → cheap**, the **integration role → standard**, **reviewers → capable** (incl. Reality Checker), **max not used as a default**, and each rationale line names the tier. *Assert the tiers exactly (cheap/standard/capable); judge only the agent-name picks for coherence.*

## S7 — tier escalation (`references/loop.md` + `references/gate.md`)
- **Input:** `loop.md` + `gate.md` + a single workstream whose first producer (dispatched at **cheap**) returns output the gate rules **needs-work** (a real High that isn't fixed); plus a `NEEDS_CONTEXT` variant.
- **Expected:** re-dispatches the fix **one tier up (cheap→standard)**, capped at `max_fix_iterations` then escalates to the human; reports the **tier path** (standard→…); the `NEEDS_CONTEXT` variant stays at **cheap** + adds context (no tier bump).

## S8 — retro learnings (`references/retro.md`)
- **Input:** `retro.md` + a mock run record (two workstreams: one passed clean; one escalated cheap→capable after a needs-work on a resource leak).
- **Expected:** emits a learnings record matching the schema, with **evidence-tagged `change` deltas** (e.g. `{target:tier, delta:"default the leak-prone role to capable", evidence:"escalated twice on the leak"}`); flags any skill-editing delta as **proposed/human-gated**; records nothing it can't cite run evidence for.

## S9 — Caster consults learnings (`references/caster.md` + `references/learnings.md`)
- **Input:** `caster.md` + a `learnings.md` seeded with one entry (`android | keep the ExoPlayer-leak check as a must-fix; default the build role to capable | evidence: escalated twice`). Task "build an Android TV app".
- **Expected:** adjusts the **mobile-dev** pick accordingly (the `android` learnings key normalizes to `mobile-dev`; build role → capable; leak check noted) **and** treats it as an **overridable default** (states it could override), not a hard rule.

## S10 — Layer B evolve dry-run (`references/evolve.md`)
- **Input:** `evolve.md` + an ai-research task + `--evolve`. Dry-run (no execution).
- **Expected:** describes the generations loop with `[methodology reviewer, Reality Checker]` per generation, a budget, the human gate, and the leakage caution — **without executing** — and confirms it's opt-in / ai-research-only.

## S11 — cross-CLI tier + dispatch resolution (`references/platforms.md`)
- **Input:** `platforms.md` + these asks: (a) resolve **tier=cheap** for Claude Code, Gemini, Codex, CodeWhale, OpenCode; (b) resolve **tier=max** for all five; (c) back-compat: resolve a role carrying the Claude name **`sonnet`** on Gemini.
- **Expected:** (a) `haiku` / `flash-lite` / `gpt-5.x-mini` / `deepseek-v4-flash` / **OpenCode → the configured provider's mini / native `small_model`** (provider-agnostic, **not pinned** — e.g. `anthropic/claude-haiku-4-5` or `openai/gpt-5-mini`), each with the right dispatch verb (Agent tool `model:` / `@generalist` frontmatter `model:` / Codex custom agent `model=` / CodeWhale subagent via config.toml / **OpenCode subagent via the `task` tool / `@<agent>` with frontmatter `model: <provider>/<id>`, or JSON under `agent` in `opencode.json`**); (b) `fable` / `gemini pro (+high)` / `gpt-5.5 (+high model_reasoning_effort)` / `deepseek-reasoner` / **OpenCode → the provider's top reasoning model (+ effort where exposed)**; (c) `sonnet→standard→gemini flash`. OpenCode tiers are **provider-resolved from `opencode.json`, never pinned to a vendor**. A single-tier pass is insufficient — ≥2 tiers + the back-compat input must hold.

## S12 — orchestration adherence (`SKILL.md` §"You are the conductor" + `references/loop.md`)
- **Input:** give a subagent the installed/fixed skill + a multi-workstream task with an *easy* workstream (e.g. GET /health + a request-id middleware + a **one-line README badge**). Ask it, as the conductor, whether it produces the easy badge workstream **inline** or **dispatches** a producer for it — and to cite the governing rule.
- **Expected:** it answers **dispatch for EVERY workstream (including the easy badge)**, cites the imperative conductor rule + the "trivial → inline" red flag that counters it, and notes the mandatory "Dispatching … for WS-N" re-anchor; if dispatch is genuinely unavailable it **says so and pauses**, never silently producing inline.

> The tier vocabulary is abstract (`cheap/standard/capable/max`, resolved per `references/platforms.md`); re-run **S1–S7** for selection/gate/escalation, **S8–S10** for the retro/learnings/evolve loop, **S11** for cross-CLI resolution, and **S12** for conductor adherence.

## S13 — minimal-code principle (native) + ponytail amplifier (`SKILL.md` + `references/loop.md`/`gate.md`/`caster.md`)
- **Input:** a code task (e.g. "add a JSON config loader to our CLI"). Consider both: (a) `ponytail` absent, (b) `ponytail` installed.
- **Expected:** in **both** cases the producer aims for the *simplest solution that fully works* and the gate's Code Reviewer flags **over-engineering** — never by cutting validation/security/accessibility — because minimal-code is **native** to dreamteam (SKILL.md Overview + loop.md + gate.md), NOT gated on the skill. When `ponytail` IS installed, the Caster additionally attaches it to the code producer's `skills[]` as the enforcer. Absent ponytail ≠ no minimal-code check.

## S14 — advisory recommend (skills-only) (`references/recommend.md` + `references/caster.md`)
- **Input:** `recommend.md` + `caster.md`; act as the Caster for a task whose best-fit skill is **discoverable-but-not-installed** (via `find-skills`/skills.sh), with a weaker skill already installed. Plus a variant where **nothing** clears the quality bar.
- **Expected:** records a `recommendations[]` entry (a **verified** candidate + `why` + `trust` + `install` cmd + **`proceeding_with` naming the alternate it ran with, or `'omitted'`**), **proceeds with the best available** installed option, and **never blocks**. In the variant where nothing clears the bar → **no** recommendation (omitted beats a weak pick). The Caster still only selects — it recommends, never installs.

## S15 — awesome-claude-code CSV (Claude-only) (`references/recommend.md` + `references/platforms.md`)
- **Input:** `recommend.md` + `platforms.md`; a **Claude-Code** task needing a **non-skill** resource `find-skills` can't surface (e.g. a hook) → the Caster discovers from `THE_RESOURCES_TABLE.csv`. Plus the same ask under a non-Claude `--platform` (gemini/codex/codewhale/opencode).
- **Expected:** the recommendation is sourced from the CSV with **`installable:false`** (the `install` field **omitted** — no synthesized `npx skills add`), **health-flag-filtered** (skip rows with `Stale=TRUE`/`Removed From Origin=TRUE`/`Active=FALSE`), with licenses surfaced honestly. Under **any non-Claude `--platform`** the Claude-specific recommendation is **suppressed**.

## S16 — opt-in installer role (`references/recommend.md` + `references/gate.md`)
- **Input:** `recommend.md` + `gate.md`; a recommended candidate **with explicit user approval** → a gated `setup` producer (`general-purpose@cheap` + `find-skills`). Cover: (a) the pinned cmd carrying no `-y`; (b) a `-y` invocation that lands the right package; (c) `--autonomy auto`; (d) a substituted/typosquatted package.
- **Expected:** (a) the `setup` producer runs the **ONE pinned approved cmd (no `-y`)**, the gate verifies **presence + identity/source + that the command carried no `-y`**, the conductor **re-casts** (≤ once per approved set) and the skill attaches; (b) a `-y` invocation is **rejected (needs-work) even if the right package landed** (the gate checks the command, not just the result); (c) `--autonomy auto` does **NOT** bypass the human approval / auto-install; (d) a substituted/typosquatted package **fails** the identity gate.

## S17 — F6 (Karpathy fold-in) (`references/loop.md` + `references/gate.md`)
- **Input:** `loop.md` + `gate.md`; a producer dispatched with an **explicit, verifiable success criterion**, whose output exhibits **both** over-engineering **and** gratuitous diff scope (unrequested refactors/reformatting of untouched code); plus a variant with **genuine ambiguity** in the spec.
- **Expected:** the dispatch carries the explicit success criterion (what the gate will check — not "make it work"); the gate raises **both** `over-engineering` **and** `gratuitous diff scope` as findings; on genuine ambiguity the producer returns **NEEDS_CONTEXT** (surface assumptions first) — **not a silent guess**.

> S14–S17 are additive (selection/gate/loop/principles unchanged; the Caster still only selects — now also recommends); re-run S1–S13 as regression.

## S18 — audit/bugs (`references/audit.md` + `references/gate.md` + `references/loop.md`)
- **Input:** `audit.md` + `gate.md` + `loop.md`; an `audit` run (mode `bugs`) over existing code — dimension reviewers (e.g. correctness/security/performance) fan out, plus a bare `--profile audit` variant with no mode given.
- **Expected:** the dimension reviewers fan out **as producers** (read-only finding-hunters, not the gate); the gate **confirms a real bug and refutes a false positive** (evidence beats prediction); the run is **read-only — `integrate` is a no-op** (no worktree merge, nothing written back); a refuted finding is **dropped from the report, not the whole report**; bare `--profile audit` defaults to **`bugs`**.

## S19 — audit/map + depth (`references/audit.md`)
- **Input:** `audit.md`; an `audit` run (mode `map`) over a repo, run at `--depth shallow` and again at `--depth exhaustive`.
- **Expected:** mode `map` produces a **structured project model** (not a bug list); `--depth shallow` vs `exhaustive` **changes fan-out breadth**; **`exhaustive` is budget-printed + confirm-gated**; fan-out is **capped by `audit_policy.max_parallel_reviewers`**.

## S20 — mutation/mock-integrity (`references/gate.md`)
- **Input:** `gate.md` + a producer's test suite that is **green**, including (a) a test that **passes against a deliberately broken implementation** and (b) a test that **mocks the very unit under test**.
- **Expected:** both are a **must-fix** — a test that **survives a broken implementation** or **mocks the unit under test** proves nothing, **even though it's "green"** (passing ≠ verifying).

## S21 — devil's-advocate-on-unanimous (`references/gate.md`)
- **Input:** `gate.md` + (a) a **3-reviewer unanimous PASS**, (b) a **2-reviewer** unanimous pass, and (c) the same 3-reviewer unanimous pass **outside the `audit` profile**.
- **Expected:** (a) the unanimous PASS triggers **one** adversarial reviewer **once** (outside the fix loop), whose **unevidenced prediction is dropped** on reconciliation; (b) a **2-reviewer** pass does **NOT** trigger it (threshold is 3); (c) it is **off by default outside the `audit` profile**.

## S22 — mobile-dev rename (`references/profiles.md`)
- **Input:** `profiles.md` + tasks "build an Android TV app", "build a React Native app", "an iOS app"; plus `--profile android`; plus a `learnings.md` row keyed `android`.
- **Expected:** all three tasks **resolve to `mobile-dev`** (keyword widening across Android/React Native/iOS, **not** the Caster fallback); `--profile android` **resolves to `mobile-dev`** (alias kept for back-compat); a learnings row keyed `android` **still matches** the renamed profile.

> S18–S22 are additive (audit profile + gate sharpenings + the mobile-dev rename); re-run S1–S17 as regression.

## S23 — execution mode (F10) (`SKILL.md` + `references/loop.md` + `references/platforms.md`)
- **Input:** a dreamteam run reaching its **first dispatch**, on **Claude Code** and (variants) on **Gemini** and **OpenCode**.
- **Expected:** the conductor dispatches in the **background** (never a foreground/inline Agent call); on the **first dispatch** it asks **once** "background subagents or Workflow? [background]" and uses that choice for the **session** (defaults to **background** if no answer); the mode is **surfaced in the re-anchor line**; the **Workflow option is offered only on Claude Code** (Gemini/Codex/CodeWhale/OpenCode → background, **no prompt**); the conductor still **awaits each result then gates** (background ≠ fire-and-forget); best-effort if the choice falls out of context (**re-ask, don't assume**). On **OpenCode**, "background" means dispatched via the native **`task` tool / `@<agent>`** (not the Workflow tool) — OpenCode's `task` dispatch is **synchronous/modal in core**, yet the conductor still awaits each result then gates, so the contract is unchanged (true non-blocking background only via a community plugin).

## S24 — session stickiness + carve-out (F11) (`SKILL.md`)
- **Input:** after a dreamteam run: (a) a second artifact-producing task; (b) a pure Q&A; (c) a genuinely tiny edit (one-line typo, **not** part of any plan); (d) a one-line change that **IS** a unit of the current plan; (e) the user says "stop using dreamteam this session".
- **Expected:** (a) runs **through dreamteam** (sticky); (b) **answered directly** (out of scope — no artifact); (c) **MAY** be inline via the carve-out **only after** the conductor **states the classification** and it passes **ALL** bounds (≲5 lines / one file / no new logic / no security-validation-a11y), any doubt → **dispatch**; (d) **dispatched** (a small *workstream* is not the carve-out); (e) stickiness **off for the session**, a later `/dreamteam` **re-arms**.

## S25 — `--execution` flag pre-sets the session mode (`SKILL.md` + `references/loop.md` + `references/platforms.md`)
- **Input:** a dreamteam run reaching its **first dispatch**: (a) `/dreamteam … --execution workflow` on **Claude Code**; (b) `/dreamteam … --execution background`; (c) `/dreamteam … --execution workflow --platform gemini`; (c′) `/dreamteam … --execution workflow --platform opencode`; (d) the **same run with no `--execution` flag**.
- **Expected:** the flag **IS the answer** to the one-time choice, so the conductor **does NOT ask** "Background subagents or the Workflow tool?" — (a) session = **Workflow** mode (Claude-only), prompt **skipped**, mode **surfaced in the first re-anchor line**; (b) session = **background**, prompt **skipped**, surfaced the same way; (c)/(c′) `workflow` is **INVALID on a non-Claude platform** (no Workflow tool) → **noted clearly + falls back to background subagents** — this holds for **`--platform gemini` (c) and `--platform opencode` (c′) alike**; (d) **no regression** — with no flag the one-time per-session prompt **still governs** (asked once, **default background**, per loop.md). The flag **only pre-sets the existing one-time session choice** — dispatch is still **always background**, there is **no per-workstream re-prompt**, and the produce→gate→fix→integrate loop is **identical**.

> S23–S25 are additive (F10 execution mode · F11 session stickiness · the `--execution` flag); re-run S1–S22 as regression.

## S26 — bundled-agent load + attribution (`.claude-plugin/plugin.json` + `install.sh`/`install.ps1` + `THIRD_PARTY_NOTICES.md`/`NOTICE`)
- **Input:** the installed plugin + its `plugin.json` `agents[]`, the `vendor/` tree, the install dep-check output, and `NOTICE` / `THIRD_PARTY_NOTICES.md`.
- **Expected:** `plugin.json` `agents[]` registers the **21 vendored agents under `vendor/`** — **12** agency-agents + **4** ECC (incl. the renames `methodology-reviewer`, `Security Engineer`) + **5** SuperClaude — and `mle-workflow` **auto-loads from `skills/mle-workflow/`** (no manifest entry); all load with **no external install**. The install dep-check lists them under **`bundled (ship with the plugin)` → `[ok bundled]`**, **never `[ ! ]`**; `depended-on` (the 5 superpowers sub-skills + `find-skills` + `ui-ux-pro-max`) and `optional`/`recommend-only` (ai-research skills; `Explore`/`general-purpose` built-in) are **separate groups**. Attribution holds: each vendored source (`vendor/{agency-agents,ecc,superclaude}/`) keeps a **verbatim upstream `LICENSE`** + a matching **`THIRD_PARTY_NOTICES.md` entry** (pinned commit + SPDX + MIT text + file list); renamed ECC agents are **body-pristine + carry an `origin:` line**; **superpowers / ui-ux-pro-max / graphify have NO notices entry** (depended-/recommend-only); root `LICENSE` is **unchanged Apache-2.0**.

## S27 — ml-dev profile (`references/profiles.md` + `references/gate.md`)
- **Input:** `profiles.md` + `gate.md` + task "build a PyTorch training pipeline with a dataset loader and an eval harness".
- **Expected:** resolves the **ml-dev** dreamteam — build → AI Engineer / **python-expert@standard** composing `[mle-workflow, ponytail, karpathy-guidelines]` · build-fix → **build-error-resolver / pytorch-build-resolver@standard** (training / CUDA errors); gate = **methodology-reviewer@capable** (ML pipeline · eval harness · data-contract) + **Reality Checker@capable** (data↔claim; eval **non-vacuous** — perturb to confirm) (+ Security Engineer@capable if infra/data-sensitive); `sequential`. Distinct from **ai-research** (research-salvage / paper / hypothesis). `mle-workflow` + `pytorch-build-resolver` are **bundled** (present by default); `python-expert` is used when bundled, else recommend-only — the gate is unchanged either way.

## S28 — debug profile (`references/profiles.md` + `references/gate.md` + `SKILL.md` spine step 2)
- **Input:** `profiles.md` + `gate.md` + `SKILL.md` + task "this test fails with a NullPointer — fix it" (a stack trace).
- **Expected:** resolves the **debug** dreamteam and — per the **SKILL.md spine step-2 carve-out (the same one as `audit`)** — **skips the wrapper + plan-writing**, going **reproduce-first** straight to investigate→fix (the failure *is* the spec). investigate → **root-cause-analyst@capable** `[systematic-debugging]` (absent → investigates natively + a `recommendations[]` entry, **never a block**) · fix → host code producer `[ponytail, karpathy-guidelines]`; gate = **Reality Checker@capable reproduce-then-resolve** (the failing case is **RED before / GREEN after** + a regression test) + Code Reviewer@capable; a "fix" whose test **passes even against the original unfixed code** is **rejected as vacuous**, never a pass; **unlike audit, debug integrates** (it lands the fix); `sequential`.

## S29 — ux-designer profile (`references/profiles.md` + `references/gate.md`)
- **Input:** `profiles.md` + `gate.md` + (a) "redesign our settings screen / audit its a11y"; (b) greenfield "design a component library from scratch".
- **Expected:** resolves the **ux-designer** dreamteam — UI Designer@standard composes `[ui-ux-pro-max]` (a **depended** skill; absent → UI Designer designs natively + a `recommendations[]` entry); (a) the **redesign** sub-role adds **deep-research-agent@standard** to read the existing UI/codebase (**NO Understand-Anything**), (b) **greenfield omits it** (N/A, not degradation); gate = **Reality Checker@capable that re-derives a11y evidence** (contrast ≥4.5:1 · visible focus · `prefers-reduced-motion` · semantic structure · spec↔rendered) + a UX/design reviewer + an accessibility reviewer, **a11y NON-WAIVABLE even when `ui-ux-pro-max` is absent**, + Code Reviewer **only if implementation code is emitted** (the gate branches **spec-only vs code-emitted**); `sequential`.

## S30 — tutor profile (`references/profiles.md` + `references/gate.md`)
- **Input:** `profiles.md` + `gate.md` + (a) "explain how TCP congestion control works / ELI5"; (b) "map this codebase / understand this repo".
- **Expected:** (a) resolves the **tutor** dreamteam — understand → **deep-research-agent@standard** · explain → **Technical Writer@standard**, **no external skill** (self-contained; Understand-Anything discarded; if deep-research-agent is recommend-only, fall back to host + `recommend.md` and flag); gate = **Reality Checker@capable explanation↔source** (every claim traces to a cited source; **SOURCE WINS on conflict** — no lies-to-children: a *false* simplification is a must-fix) + a clarity reviewer (reading level · jargon defined on first use · ≥1 worked example), **accuracy gating first**; `sequential`. (b) "map the codebase / understand this repo" stays → **audit**, **not** tutor.

## S31 — karpathy-guidelines composed onto code producers (`SKILL.md` Composes + `references/caster.md` + `references/profiles.md`)
- **Input:** a code task ("add a typed retry helper to our HTTP client"); consider (a) `karpathy-guidelines` absent, (b) installed.
- **Expected:** like `ponytail`, **code-producing producers compose `karpathy-guidelines`** (surface assumptions · surgical/minimal-diff changes · verifiable success criteria), attached to their `skills[]` on **both the fast-path and the agent path**, each `find-skills`-gated **independently**; (a) absent → the producer works natively and the principle **still holds** (its guidelines already ride in `loop.md`/`gate.md`) — **degrades gracefully, never a block**; (b) installed → attached as the explicit enforcer **alongside `ponytail`**. Non-code roles (ai-research *writing*, tutor, ux-designer *design* sub-role) do **not** carry the code enforcers.

## S32 — no-glazing / objective standard (`SKILL.md` + `references/gate.md` §3 + `references/caster.md` dispatch brief)
- **Input:** `gate.md` + `SKILL.md` + `caster.md` + a mock review that returns a praise-padded **"looks good / LGTM"** with no specific finding (variant: a real Critical softened into a gentle suggestion).
- **Expected:** a bare "looks good" with no specific finding is **not an assessment and not a passing review** (non-conformant); **severity is set by impact, not tone** — a Critical stays Critical amid strong work, and a fix-then-pass is **not rounded up to a pass**; this **no-glazing / objective** standard is **distinct from the evidence-honesty rule** (tone vs proof — both required, neither substitutes); it **rides on every reviewer dispatch via the Caster dispatch brief**, so it binds **whatever agent is cast (vendored or host)** — **vendored bodies stay pristine**, the standard is never written into the agent file; the **conductor's own synthesis** also reports each verdict plainly (no softening).

## S33 — verification-loop 6-phase gate appendix (`references/gate.md` §5)
- **Input:** `gate.md` + a code workstream; ask the verifying reviewer to run the §5 checklist on a stack where one phase **genuinely doesn't apply** (e.g. `types` on an untyped script).
- **Expected:** the appendix runs, **in order**, **build → types → lint → tests+coverage → secret-scan → diff-review → PASS/FAIL**, each with **per-phase evidence**; coverage comes from a **real run** + non-vacuous tests; the inapplicable phase is **labelled `skipped` with a reason — never silently faked green**; **PASS only when every *applicable* phase passes**, any failing phase is a must-fix; it is a checklist the verifier **MAY** run, **not new gate machinery** — the verdict still comes from §2; the **ECC attribution credit** ("Adapted from ECC verification-loop, MIT © 2026 Affaan Mustafa; attribution in `THIRD_PARTY_NOTICES.md`") is **present**.

## S34 — graphify `--graph on|off|auto` (`SKILL.md` + `references/loop.md` §Graph + `references/caster.md`)
- **Input:** `SKILL.md` + `loop.md` + a dev run; cover `--graph auto` (graphify installed / absent / oversized repo), `--graph off`, `--graph on`.
- **Expected:** the graph is **AST-only** (deterministic → free to build/refresh; graphify's semantic/LLM extraction is **not** used); **G0 init** once at run start = presence-check → reuse-if-fresh → build/refresh (`graphify` / `graphify --update`); **G1 refresh** rides each `integrate` as a code-only (free) `graph-update` sub-step that is **not gated** (a failed refresh is a warning, not a workstream failure); **skip-if-absent** — graphify missing / `--graph off` / oversized repo → **skip silently and continue** (the Caster may record a `recommendations[]` entry), **never auto-installed, never blocks or fails the run**; the graph is **conductor infra — never a workstream (never in `producers[]`), never gated, and NEVER decides a verdict**: any graph-derived gating claim is **verified against live code/tests** (`INFERRED`/`AMBIGUOUS` edges are hints, not facts); `--graph off` disables; default `auto`.

## S35 — graphify-backed audit (`references/audit.md` + `references/gate.md`)
- **Input:** `audit.md` + `gate.md` + an `audit` run with `graphify` present, both `--mode map` and `--mode bugs`.
- **Expected:** the graph is **built once at fan-out start** (loop.md G0) and shared by every specialist; because audit's `integrate` is a no-op there is **no G1**. **`map` = the graph is the substrate**: `GRAPH_REPORT` communities → **module inventory** · god nodes → **key components / entry points** · cross-community bridges + edge density → **dependencies / risk hotspots**, and the synthesizer **folds the `GRAPH_REPORT` in** rather than rebuilding it. **`bugs` = navigation / triage only**: specialists use god nodes / call-paths / blast-radius to *locate* suspects, but a **graph-derived finding is an unconfirmed candidate** — the Reality Checker **reproduces it against live source** before it can be confirmed; a graph-only candidate that can't be reproduced is **dropped** like any other. Absent / `--graph off` / oversized → specialists read the tree directly, the report **still ships** (a `recommendations[]` entry); the graph **never decides a finding's verdict**.

## S36 — recommend the 6 component upstreams (`references/recommend.md` + `references/platforms.md`)
- **Input:** `recommend.md` + `platforms.md`; the Caster hits a gap needing a component beyond the vendored subset (an extra agent / skill / command); plus a non-Claude `--platform` variant.
- **Expected:** all **six upstreams** are surfaced with **real commands carrying no `-y`/auto-confirm**, each labelled by role — **agency-agents** (MIT, *vendored-subset → pull more agents*), **ECC** (MIT, *vendored-subset → extra reviewers/resolvers/skills/commands*), **SuperClaude** (MIT, *vendored-subset → broader `sc:*` suite*), **superpowers** (MIT, ***depended-on* → install the 5 sub-skills + systematic-debugging**), **ui-ux-pro-max** (MIT, ***depended-on* → ux-designer / design roles**), **graphify** (MIT, ***recommend-only*, never vendored**); the Claude-only `/plugin …` form **falls back to git/npx/pip under a non-Claude `--platform`** (the Claude-only form suppressed); these plugin/git/npm/pip sources stay on the **recommend-and-print** path (**not** the one-pinned `npx skills add` auto-`setup` flow). The named **optional reviewer lenses** (silent-failure-hunter · type-design-analyzer · comment-analyzer · code-simplifier/refactor-cleaner — **host-present only, name-don't-vendor**) and the **ECC recommend-targets** (search-first · skill-stocktake · context-budget — **awareness only**) are surfaced as recommend-only. The Caster **recommends, never installs**.

## S37 — learnings project_key + numeric confidence (`references/retro.md` + `references/learnings.md` + `references/caster.md` step 0)
- **Input:** `retro.md` + `learnings.md` + `caster.md`; (a) a retro emitting a record on a repo **with** a git remote; (b) a repo with **no** remote / a deliberate cross-project insight; (c) the Caster consulting a `learnings.md` seeded with one **global** entry + one **project** entry from a *different* project + one from the **current** project.
- **Expected:** the retro stamps each record with **`project_key`** = a stable hash of the repo's **git-remote (origin) URL**, and a **numeric `confidence` 0.3–0.9** (replacing `low|med|high`) — start **0.3–0.5**, **+ per corroborating run up to cap 0.9** (never 1.0), **− per contradicting run / staleness down to floor 0.3**, **below 0.3 dropped, not stored**; a repo with no remote (or a deliberate cross-project insight) is stamped **`global`**; the store gains a `project_key` column. The Caster (precedence **step 0**) consults **GLOBAL entries (`project_key` = `global`, or a blank/legacy cell) always + PROJECT entries whose `project_key` == the current repo's git-remote hash** — the *different*-project entry **never leaks in**; a blank/legacy cell is treated as **global (back-compat)**; matched entries are **overridable defaults weighted by their numeric confidence**, never hard rules; `profile` stays the **primary match key** (+ `task_kind` loose-matched), and the **`android → mobile-dev` normalization still matches**; optional human-gated **project→global promotion** at 2+ distinct projects with avg confidence ≥0.8.

## S38 — opt-in dependency installer (`install.sh` + `install.ps1`)
- **Input:** a scratch-`HOME` dry-run of `install.sh` / `install.ps1` with a depended-on dependency missing; cover interactive **`y`**, interactive **`N`**, and **non-interactive**.
- **Expected:** after the dep-check the installer **prints the registered install commands** (superpowers + `find-skills` + `ui-ux-pro-max` — same as README Step 1) and, **only when a depended-on item is missing AND the shell is interactive**, prompts **`Install the required dependencies now? [y/N]`** defaulting **`N`**; on **`y`** it runs the registered commands with **no `-y`/auto-confirm flag** (reporting per-command failures without aborting); on **`N`** it prints `skipped (default N)` and continues; **non-interactive → does not prompt** (prints the commands + continues). It is **strictly opt-in — never auto-installs**, never blocks the skill install, and the **bundled agents + `mle-workflow` are never in the missing / prompt set** (they ship with the plugin).

## S39 — marketplace install path (`README.md` Install + `.claude-plugin/marketplace.json` + `install.sh`/`install.ps1`)
- **Input:** the README Install section + `marketplace.json` + the installers; a user installing via the published Claude Code plugin marketplace, plus the manual fallback.
- **Expected:** the published path is **`/plugin marketplace add adnantaufique/dreamteam`** then **`/plugin install dreamteam@dreamteam-marketplace`**; `marketplace.json` declares **`source: "./"`**, so the package ships the whole bundle (`vendor/` + `skills/` + `plugin.json`); the README states it is **available now that the repo is public** and **honestly notes it has not been client-tested yet**, with **`install.sh` / `install.ps1` as the always-works manual fallback** (the manual path mirrors `skills/dreamteam` → `~/.claude/skills/dreamteam` and runs the dep-check); both paths land the **same bundle**.

## S40 — OpenCode platform (native skill discovery + dispatch + execution) (`references/platforms.md` + `scripts/sync-to-opencode.{sh,ps1}` + `SKILL.md`)
- **Input:** `platforms.md` + `SKILL.md` + the `sync-to-opencode` scripts; (a) a dreamteam run on **`--platform opencode`** (or auto-detected via a `~/.config/opencode` marker); (b) a host that has only run the Claude `install.sh`/`install.ps1` (no opencode sync); (c) the same on a host that ran `sync-to-opencode`.
- **Expected:** dreamteam loads as a **native OpenCode Agent Skill**, discovered on-demand via the native **`skill` tool** — **NOT** an AGENTS.md downgrade (OpenCode's `SKILL.md` format is Claude-compatible; `name` + `description` frontmatter is exactly its required schema). (b) Because OpenCode natively reads **`~/.claude/skills/<name>/SKILL.md`**, the skill is **auto-discovered with no sync step**; (c) `sync-to-opencode.{sh,ps1}` mirrors `SKILL.md` + `references/` into **`~/.config/opencode/skills/dreamteam/`** (the loader injects `SKILL.md` only; `references/*.md` are read on demand via the `read` tool — same on-disk mechanism as the other non-Claude syncs). Dispatch resolves to an **OpenCode subagent via the `task` tool / `@<agent>`** (frontmatter `model: <provider>/<id>`, or JSON under `agent` in `opencode.json`); tiers are **provider-agnostic / BYO-provider — never pinned** (cheap = the provider's mini / native `small_model`; max = its top reasoning model). Execution is the **background bucket — no Workflow tool** (so `--execution workflow --platform opencode` is invalid → note + background fallback), with the honest caveat that OpenCode's `task` dispatch is **synchronous/modal in core** (true non-blocking background only via a community plugin), yet dispatch→await→gate→integrate is unchanged. Global-instructions fallback is `~/.config/opencode/AGENTS.md` (or the shared root `AGENTS.md`, read natively); **web-search is a native Exa-powered `websearch` tool, off by default (needs the OpenCode provider or `OPENCODE_ENABLE_EXA`)** while `webfetch` is native. Auto-detect uses the **`~/.config/opencode` marker, checked before the `~/.agents`→codex fallback**. The 4 existing platforms + the loop/gate/spine contract are **unchanged**.

> S1–S25 are re-run as regression — S26–S40 are additive (S26–S39 verify the Phase A–H bundle build; **S40 verifies the OpenCode 5th-platform wiring**). S37 is the dedicated check for the `project_key` + numeric-confidence learnings schema that S8/S9/S22 also rely on.

## Grounding A — Android build (dry-run)
- **Input:** installed skill; `/dreamteam "build an Android TV app from its spec"`. Selection + description only.
- **Expected:** picks the **mobile-dev** profile dreamteam (Mobile App Builder + UI Designer); **sequential** workstreams; gate **[Code Reviewer, Reality Checker]**.

## Grounding B — ML research salvage (dry-run)
- **Input:** installed skill; `/dreamteam --profile ai-research "salvage a failed-hypothesis study: expand new directions ∥ polish current findings for publication"`. Selection + description only.
- **Expected:** picks the **ai-research** dreamteam; **two parallel workstreams** (*expand* ∥ *polish*); gate **[a methodology reviewer, Reality Checker]**.
